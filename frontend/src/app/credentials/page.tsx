import { CredentialAttackScreen } from "@/components/CredentialAttackScreen";

export const metadata = { title: ":credentials — HackPit" };

export default function Page() {
  return <CredentialAttackScreen />;
}
