[project]
name = "hackpit-pipeline"
version = "0.0.1"
description = "HackPit knowledge ingestion & normalization pipeline"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2",
    "pyyaml>=6",
]

[tool.uv]
package = false
