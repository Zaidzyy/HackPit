# pipeline

Knowledge ingestion & normalization for HackPit.

Reads raw knowledge sources from `/sources` (gitignored, never committed) and
builds the normalized knowledge index into `/data` (gitignored). This package
is an empty scaffold for now and will be implemented in a later phase.
